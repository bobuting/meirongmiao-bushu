---
phase: 04-entry-point-integration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: true
requirements: [ENTR-03]
user_setup: []
must_haves:
  truths:
    - "ENTR-03 (广场入口) 标记为 NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH"
    - "文档记录与 Phase 2 SquareRouteAdapter 和 Phase 3 mapToSquareResult 状态对称"
    - "未来重构建议已记录"
  artifacts:
    - path: "无文件修改"
      provides: "Documentation only"
  key_links:
    - from: "Phase 2"
      to: "SquareRouteAdapter"
      via: "NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH"
    - from: "Phase 3"
      to: "mapToSquareResult"
      via: "NOT_IMPLEMENTED placeholder"
---

<objective>
记录 ENTR-03（广场入口）的 NOT_IMPLEMENTED 状态，与 Phase 2 SquareRouteAdapter 和 Phase 3 mapToSquareResult 的状态保持对称（per D-01, D-02, D-03）。

Purpose: 文档化架构不兼容决策，为未来重构提供指引
Output: ROADMAP.md 更新，记录 ENTR-03 NOT_IMPLEMENTED 状态
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entry-point-integration/04-CONTEXT.md
@.planning/phases/04-entry-point-integration/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 ROADMAP.md 记录 ENTR-03 NOT_IMPLEMENTED 状态</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 section, Coverage table)
    - .planning/phases/04-entry-point-integration/04-CONTEXT.md (D-01, D-02, D-03 decisions)
  </read_first>
  <behavior>
    - ROADMAP.md Phase 4 Success Criteria 更新，标记 ENTR-03 为 NOT_IMPLEMENTED
    - Coverage 表格更新 ENTR-03 状态为 NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH
    - 添加 Symmetry 说明（与 Phase 2/3 保持一致）
  </behavior>
  <action>
    更新 .planning/ROADMAP.md 的 Phase 4 部分：

1. 更新 Success Criteria（约 lines 120-126），修改第 3 条：

```markdown
**Success Criteria** (what must be TRUE):
1. sync-service.ts 使用 BatchSyncAdapter + 核心管道，移除重复代码
2. single-reverse-service.ts 使用 CloneAdapter + 核心管道，移除重复代码
3. ENTR-03 (广场入口) 标记为 NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH（与 Phase 2/3 SquareRouteAdapter/mapToSquareResult 状态对称）
4. 核心反推逻辑维护点唯一（video-reverse-core 模块）
```

注意：移除原第 4 条"所有三个入口点通过 Phase 0 特征测试验证"，改为上述第 3 条。

2. 更新 Coverage 表格（约 lines 145-160），添加 ENTR-03 状态：

```markdown
| ENTR-03 | Phase 4 | NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH |
```

3. 在 Coverage 表格后添加 Symmetry 说明：

```markdown
**ENTR-03 Symmetry:**
- Phase 2: SquareRouteAdapter = NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH
- Phase 3: mapToSquareResult = NOT_IMPLEMENTED placeholder
- Phase 4: reverse-parse-routes.ts integration = deferred
- Future: Requires ReverseParseRouteDeps converted to function-style interface
```
</action>
  <acceptance_criteria>
    - `grep -n "NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH" .planning/ROADMAP.md` 返回至少 1 行
    - `grep -n "ENTR-03" .planning/ROADMAP.md` 返回至少 2 行（Coverage + Symmetry）
    - `grep -n "SquareRouteAdapter" .planning/ROADMAP.md` 返回至少 1 行（Symmetry 说明）
  </acceptance_criteria>
  <verify>
    <automated>cat .planning/ROADMAP.md | grep -c "ENTR-03"</automated>
  </verify>
  <done>
    ROADMAP.md 已更新，ENTR-03 NOT_IMPLEMENTED 状态已记录
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

无文件修改，仅文档更新。无安全威胁。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | Documentation only | accept | No security impact |
</threat_model>

<verification>
- ROADMAP.md 包含 ENTR-03 NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH 状态
- Symmetry 说明完整（Phase 2/3/4 状态一致）
</verification>

<success_criteria>
- ROADMAP.md 记录 ENTR-03 NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH
- 与 Phase 2 SquareRouteAdapter 和 Phase 3 mapToSquareResult 状态对称
- 未来重构建议已记录
</success_criteria>

<output>
After completion, create `.planning/phases/04-entry-point-integration/04-03-SUMMARY.md`
</output>