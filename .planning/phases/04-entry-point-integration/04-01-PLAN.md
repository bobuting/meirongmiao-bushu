---
phase: 04-entry-point-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/video-hot-trend/sync-service.ts]
autonomous: true
requirements: [ENTR-01]
user_setup: []
must_haves:
  truths:
    - "sync-service.ts processVideo 调用 runCoreReversePipeline"
    - "sync-service.ts processVideo 使用 createBatchReverseAdapter 创建适配器"
    - "sync-service.ts processVideo 使用 mapToBatchResult 映射输出"
    - "sync-service.ts OSS 上传逻辑保留"
    - "重复代码（下载、LLM 调用、JSON 解析）已删除"
  artifacts:
    - path: "src/modules/video-hot-trend/sync-service.ts"
      provides: "processVideo function rewritten"
      contains: "runCoreReversePipeline"
      contains: "createBatchReverseAdapter"
      contains: "mapToBatchResult"
  key_links:
    - from: "src/modules/video-hot-trend/sync-service.ts"
      to: "src/modules/video-reverse-core/unified-reverse-core.ts"
      via: "import runCoreReversePipeline"
      pattern: "import.*runCoreReversePipeline"
    - from: "src/modules/video-hot-trend/sync-service.ts"
      to: "src/modules/video-reverse-core/batch-reverse-adapter.ts"
      via: "import createBatchReverseAdapter"
      pattern: "import.*createBatchReverseAdapter"
    - from: "src/modules/video-hot-trend/sync-service.ts"
      to: "src/modules/video-reverse-core/mapper.ts"
      via: "import mapToBatchResult"
      pattern: "import.*mapToBatchResult"
---

<objective>
重写 sync-service.ts 的 processVideo 函数，使其委托给核心管道，删除重复代码，保留入口特有的 OSS 上传逻辑（per D-05, D-07, D-08）。

Purpose: 统一反推核心管道入口，实现单一维护点
Output: processVideo 使用 runCoreReversePipeline + BatchSyncAdapter + mapToBatchResult
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entry-point-integration/04-CONTEXT.md
@.planning/phases/04-entry-point-integration/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/modules/video-reverse-core/types.ts:
```typescript
export interface CoreReverseInput {
  videoUrl: string;
  topicLabel?: string;
  topicId?: string;
  routeKeys: ProviderRouteKey[];
  auditContext: CoreReverseAuditContext;
}

export interface CoreReverseOutput {
  rawLlmOutput: unknown | null;
  resolvedVideoUrl: string;
  success: boolean;
  errorCode: CoreReverseErrorCode | null;
  errorMessage: string | null;
}

export interface CoreReverseAuditContext {
  routeKey: ProviderRouteKey;
  businessContext: string;
  projectId?: string;
  userId?: string;
}
```

From src/modules/video-reverse-core/mapper.ts:
```typescript
export interface BatchSyncMapperInput {
  coreOutput: CoreReverseOutput;
  videoKey: string;
  videoTitle: string;
  rank: number;
  sourceUrl: string;
  ossUrl: string | null;
}

export function mapToBatchResult(input: BatchSyncMapperInput): LlmReverseResult
```

From src/modules/video-reverse-core/unified-reverse-core.ts:
```typescript
export async function runCoreReversePipeline(
  deps: UnifiedReverseDeps,
  input: CoreReverseInput,
): Promise<CoreReverseOutput>
```

From src/modules/video-reverse-core/batch-reverse-adapter.ts:
```typescript
export function createBatchReverseAdapter(existingDeps: VideoHotTrendSyncDeps): UnifiedReverseDeps
```

From src/modules/video-hot-trend/sync-service.ts (lines 56, 234-397):
```typescript
const ROUTE_KEY_HOT_TREND_LABELING = "hot_trend_labeling" as const;

// Current processVideo function signature
async function processVideo(rankedVideo: RankedVideo): Promise<LlmReverseResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加核心模块导入语句</name>
  <files>src/modules/video-hot-trend/sync-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/sync-service.ts (current imports section, lines 1-45)
    - src/modules/video-reverse-core/index.ts (exports to import)
  </read_first>
  <behavior>
    - Import statement added for runCoreReversePipeline, createBatchReverseAdapter, mapToBatchResult
    - Import statement added for CoreReverseInput, CoreReverseAuditContext types
  </behavior>
  <action>
    在 sync-service.ts 文件顶部的导入区域（约 lines 32-44 之后），添加以下导入语句：

```typescript
// 核心反推管道（Phase 1）
import {
  runCoreReversePipeline,
  createBatchReverseAdapter,
  mapToBatchResult,
} from "../video-reverse-core/index.js";
import type {
  CoreReverseInput,
  CoreReverseAuditContext,
} from "../video-reverse-core/types.js";
```

注意：
- 使用 `../video-reverse-core/index.js` 作为导入路径（ESM 模块）
- 类型导入使用 `import type` 语法
- 添加中文注释说明来源
</action>
  <acceptance_criteria>
    - `grep -n "runCoreReversePipeline" src/modules/video-hot-trend/sync-service.ts` 返回至少 1 行
    - `grep -n "createBatchReverseAdapter" src/modules/video-hot-trend/sync-service.ts` 返回至少 1 行
    - `grep -n "mapToBatchResult" src/modules/video-hot-trend/sync-service.ts` 返回至少 1 行
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    导入语句已添加，TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 重写 processVideo 函数使用核心管道</name>
  <files>src/modules/video-hot-trend/sync-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/sync-service.ts (processVideo function, lines 234-397)
    - src/modules/video-reverse-core/unified-reverse-core.ts (runCoreReversePipeline implementation)
    - src/modules/video-reverse-core/mapper.ts (mapToBatchResult function)
    - src/modules/video-reverse-core/batch-reverse-adapter.ts (createBatchReverseAdapter function)
  </read_first>
  <behavior>
    - processVideo 调用 runCoreReversePipeline
    - processVideo 使用 createBatchReverseAdapter(deps)
    - processVideo 使用 mapToBatchResult 映射输出
    - OSS 上传逻辑保留（下载视频单独用于 OSS，核心管道内部会重新下载）
    - 重复代码删除（download、LLM call、JSON parse、normalizeLlmReverseOutput）
    - 错误处理：返回 failed result（不抛异常，批量入口语义）
  </behavior>
  <action>
    完全重写 processVideo 函数（约 lines 234-397），替换为以下实现：

```typescript
    /**
     * 执行 LLM 分镜反推（使用统一核心管道）
     * 删除重复代码，保留入口特有 OSS 上传逻辑 (per D-08)
     */
    async function processVideo(rankedVideo: RankedVideo): Promise<LlmReverseResult> {
      const videoKey = normalizeHotTrendKey("video", rankedVideo.video.label);
      const sourceUrl = resolveVideoSourceUrl(rankedVideo.video);

      // 必须有视频 URL
      if (!sourceUrl) {
        return {
          videoKey,
          videoTitle: rankedVideo.video.label,
          rank: rankedVideo.rank,
          sourceUrl: "",
          ossUrl: null,
          status: "failed",
          output: null,
          errorCode: "NO_VIDEO_URL",
          errorMessage: "video has no source url",
        };
      }

      // ---- 入口特有逻辑：OSS 上传 (per D-08) ----
      // OSS 上传需要视频 base64，核心管道内部也会下载视频用于 LLM
      // 两次下载在批量场景下是可接受的代价
      const ossDownloadResult = await deps.downloadVideoForLlm(sourceUrl);
      const ossUploadPromise = ossDownloadResult
        ? deps.uploadVideoToOss(
            ossDownloadResult.base64,
            ossDownloadResult.mimeType,
            `hot-trend-video/${String(rankedVideo.video.id ?? rankedVideo.rank)}`
          ).catch((err) => {
            deps.log.warn({ err, sourceUrl }, "video hot trend: async oss upload failed");
            return null;
          })
        : Promise.resolve(null);

      // ---- 核心管道调用 ----
      const adapter = createBatchReverseAdapter(deps);
      const coreInput: CoreReverseInput = {
        videoUrl: sourceUrl,
        topicLabel: rankedVideo.video.label,
        topicId: String(rankedVideo.video.id ?? rankedVideo.rank),
        routeKeys: [ROUTE_KEY_HOT_TREND_LABELING],
        auditContext: {
          routeKey: ROUTE_KEY_HOT_TREND_LABELING,
          businessContext: "视频热榜 LLM 反推",
        },
      };

      const coreOutput = await runCoreReversePipeline(adapter, coreInput);

      // ---- 等待 OSS 上传完成 ----
      const ossUrl = await ossUploadPromise;

      // ---- 映射输出 ----
      return mapToBatchResult({
        coreOutput,
        videoKey,
        videoTitle: rankedVideo.video.label,
        rank: rankedVideo.rank,
        sourceUrl,
        ossUrl,
      });
    }
```

关键点：
1. OSS 上传逻辑保留：先下载视频用于 OSS，启动异步上传
2. 核心管道会内部重新下载视频用于 LLM（两次下载可接受）
3. 使用 createBatchReverseAdapter(deps) 创建适配器
4. 使用 mapToBatchResult 映射 CoreReverseOutput 到 LlmReverseResult
5. 批量入口错误语义：返回 failed result，不抛异常
6. 删除原有代码：download、LLM dispatch、JSON parse、normalizeLlmReverseOutput
</action>
  <acceptance_criteria>
    - `grep -n "runCoreReversePipeline" src/modules/video-hot-trend/sync-service.ts` 返回至少 2 行（import + call）
    - `grep -n "createBatchReverseAdapter" src/modules/video-hot-trend/sync-service.ts` 返回至少 2 行（import + call）
    - `grep -n "mapToBatchResult" src/modules/video-hot-trend/sync-service.ts` 返回至少 2 行（import + call）
    - `grep -n "uploadVideoToOss" src/modules/video-hot-trend/sync-service.ts` 返回至少 1 行（OSS 逻辑保留）
    - `grep -n "normalizeLlmReverseOutput" src/modules/video-hot-trend/sync-service.ts` 返回 0 行或仅有 export 行（重复调用删除）
    - `grep -n "extractJsonValue" src/modules/video-hot-trend/sync-service.ts` 返回 0 行或仅有其他上下文（重复调用删除）
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    processVideo 函数已重写，使用核心管道，保留 OSS 上传逻辑，删除重复代码
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除 runLlmReversePhase 中对 provider 的传递（processVideo 不再需要）</name>
  <files>src/modules/video-hot-trend/sync-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/sync-service.ts (runLlmReversePhase function, lines 219-414)
  </read_first>
  <behavior>
    - runLlmReversePhase 函数签名简化（移除 provider 和 providerRoute 参数）
    - processVideo 调用不再依赖外部传入的 provider
    - 统计逻辑保留
  </behavior>
  <action>
    runLlmReversePhase 函数签名需要简化，因为 processVideo 现在内部使用核心管道处理 provider 解析：

修改 runLlmReversePhase 函数签名（约 line 219）：
```typescript
async function runLlmReversePhase(input: {
  rankedVideos: RankedVideo[];
  // 移除 provider 和 providerRoute，核心管道内部处理
  // provider: ResolvedRouteProvider;  <-- 删除
  // providerRoute: ProviderRouteKey;  <-- 删除
  topN: number;
}): Promise<{
  results: LlmReverseResult[];
  stats: { selected: number; succeeded: number; failed: number };
}>
```

注意：runLlmReversePhase 中的 provider 参数原本用于传递给 processVideo，现在 processVideo 内部通过核心管道处理，所以移除这些参数。

同时需要修改 sync 函数中调用 runLlmReversePhase 的代码（约 lines 600-608），移除 provider 和 providerRoute 的传递：

```typescript
// 原代码：
const reversePhaseResult = await runLlmReversePhase({
  rankedVideos,
  provider: providerResolution.provider,  // <-- 删除这行
  providerRoute: providerResolution.routeKey,  // <-- 删除这行
  topN,
});

// 新代码：
const reversePhaseResult = await runLlmReversePhase({
  rankedVideos,
  topN,
});
```

注意：
- processVideo 内部的核心管道会自己解析 provider（通过 adapter.resolveProvider）
- sync 函数中的 providerResolution 检查逻辑保留（用于判断是否有可用 provider）
</action>
  <acceptance_criteria>
    - `grep -n "provider:" src/modules/video-hot-trend/sync-service.ts` 在 runLlmReversePhase call 处返回 0 行
    - `grep -n "providerRoute:" src/modules/video-hot-trend/sync-service.ts` 在 runLlmReversePhase call 处返回 0 行
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    runLlmReversePhase 简化完成，processVideo 不依赖外部 provider
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync-service → core pipeline | 依赖注入边界，deps 通过 adapter 传入 |
| core pipeline → LLM provider | 外部服务调用，需 timeout 保护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | processVideo input | mitigate | Core pipeline validates videoUrl before download |
| T-04-02 | Denial of Service | LLM call timeout | mitigate | 180_000ms timeout enforced in core pipeline |
| T-04-03 | Information Disclosure | OSS upload failure | accept | OSS failure logged but not blocking (async) |
</threat_model>

<verification>
- TypeScript 编译通过（npm run build）
- 导入语句正确
- processVideo 函数调用 runCoreReversePipeline
- OSS 上传逻辑保留
- 重复代码删除
</verification>

<success_criteria>
- processVideo 使用 runCoreReversePipeline + createBatchReverseAdapter + mapToBatchResult
- OSS 上传逻辑保留（downloadVideoForLlm + uploadVideoToOss）
- 删除重复代码：download（核心管道内部）、LLM dispatch、JSON parse、normalizeLlmReverseOutput 调用
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-entry-point-integration/04-01-SUMMARY.md`
</output>