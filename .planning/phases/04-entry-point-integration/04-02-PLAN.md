---
phase: 04-entry-point-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/video-hot-trend/single-reverse-service.ts]
autonomous: true
requirements: [ENTR-02]
user_setup: []
must_haves:
  truths:
    - "single-reverse-service.ts runSingleVideoLlmReverse 调用 runCoreReversePipeline"
    - "single-reverse-service.ts 使用 createCloneAdapter 创建适配器"
    - "single-reverse-service.ts 使用 mapToCloneResult 映射输出"
    - "用户入口错误处理：失败时抛出 AppError"
    - "重复代码（下载、LLM 调用、JSON 解析、normalizeLlmReverseOutput）已删除"
    - "mapLlmReverseToResult 函数已删除（由 mapToCloneResult 替代）"
  artifacts:
    - path: "src/modules/video-hot-trend/single-reverse-service.ts"
      provides: "runSingleVideoLlmReverse function rewritten"
      contains: "runCoreReversePipeline"
      contains: "createCloneAdapter"
      contains: "mapToCloneResult"
  key_links:
    - from: "src/modules/video-hot-trend/single-reverse-service.ts"
      to: "src/modules/video-reverse-core/unified-reverse-core.ts"
      via: "import runCoreReversePipeline"
      pattern: "import.*runCoreReversePipeline"
    - from: "src/modules/video-hot-trend/single-reverse-service.ts"
      to: "src/modules/video-reverse-core/clone-adapter.ts"
      via: "import createCloneAdapter"
      pattern: "import.*createCloneAdapter"
    - from: "src/modules/video-hot-trend/single-reverse-service.ts"
      to: "src/modules/video-reverse-core/mapper.ts"
      via: "import mapToCloneResult"
      pattern: "import.*mapToCloneResult"
---

<objective>
重写 single-reverse-service.ts 的 runSingleVideoLlmReverse 函数，使其委托给核心管道，删除重复代码和 mapLlmReverseToResult 函数（per D-06, D-07）。

Purpose: 统一反推核心管道入口，实现单一维护点
Output: runSingleVideoLlmReverse 使用 runCoreReversePipeline + CloneAdapter + mapToCloneResult
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entry-point-integration/04-CONTEXT.md
@.planning/phases/04-entry-point-integration/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/modules/video-reverse-core/types.ts:
```typescript
export interface CoreReverseInput {
  videoUrl: string;
  topicLabel?: string;
  topicId?: string;
  routeKeys: ProviderRouteKey[];
  auditContext: CoreReverseAuditContext;
}

export interface CoreReverseOutput {
  rawLlmOutput: unknown | null;
  resolvedVideoUrl: string;
  success: boolean;
  errorCode: CoreReverseErrorCode | null;
  errorMessage: string | null;
}

export const CORE_REVERSE_ERROR_CODES = {
  VIDEO_DOWNLOAD_FAILED: "VIDEO_DOWNLOAD_FAILED",
  NO_PROVIDER: "NO_PROVIDER",
  LLM_RESPONSE_INVALID: "LLM_RESPONSE_INVALID",
  LLM_CALL_FAILED: "LLM_CALL_FAILED",
} as const;
```

From src/modules/video-reverse-core/mapper.ts:
```typescript
export interface CloneMapperInput {
  coreOutput: CoreReverseOutput;
  videoUrl: string;
  generateId: () => string;
  now: () => number;
}

export function mapToCloneResult(input: CloneMapperInput): SingleVideoReverseResult
```

From src/modules/video-reverse-core/unified-reverse-core.ts:
```typescript
export async function runCoreReversePipeline(
  deps: UnifiedReverseDeps,
  input: CoreReverseInput,
): Promise<CoreReverseOutput>
```

From src/modules/video-reverse-core/clone-adapter.ts:
```typescript
export function createCloneAdapter(existingDeps: SingleVideoReverseDeps): UnifiedReverseDeps
```

From src/core/errors.ts:
```typescript
export class AppError extends Error {
  constructor(status: number, code: string, message: string);
}
```

From src/modules/video-hot-trend/single-reverse-service.ts (lines 217-332):
```typescript
export async function runSingleVideoLlmReverse(
  deps: SingleVideoReverseDeps,
  videoUrl: string,
): Promise<SingleVideoReverseResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加核心模块导入语句并移除旧导入</name>
  <files>src/modules/video-hot-trend/single-reverse-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/single-reverse-service.ts (current imports section, lines 1-25)
    - src/modules/video-reverse-core/index.ts (exports to import)
  </read_first>
  <behavior>
    - Import runCoreReversePipeline, createCloneAdapter, mapToCloneResult from video-reverse-core
    - Import CoreReverseInput, CoreReverseAuditContext, CORE_REVERSE_ERROR_CODES types
    - Remove import of normalizeLlmReverseOutput from sync-service.js (no longer used)
    - Remove import of extractJsonValue from utils/json.js (no longer used directly)
    - Remove import of buildVideoStoryboardPrompt from prompt.js (core pipeline handles)
    - Remove import of getPromptContent from prompt-helper.js (core pipeline handles)
    - Remove PROMPT_CODE_VIDEO_STORYBOARD_ANALYSIS constant (core pipeline handles)
  </behavior>
  <action>
    修改 single-reverse-service.ts 文件顶部的导入区域：

**删除以下导入（不再需要）：**
```typescript
// 删除这些导入
import { extractJsonValue } from "../../utils/json.js";  // 核心管道通过 adapter 处理
import { buildVideoStoryboardPrompt } from "./prompt.js";  // 核心管道处理
import { normalizeLlmReverseOutput } from "./sync-service.js";  // 核心管道处理
import { getPromptContent } from "../prompt/prompt-helper.js";  // 核心管道处理

// 删除这个常量
const PROMPT_CODE_VIDEO_STORYBOARD_ANALYSIS = "video_storyboard_analysis";
```

**添加以下导入：**
```typescript
// 核心反推管道（Phase 1-3）
import {
  runCoreReversePipeline,
  createCloneAdapter,
  mapToCloneResult,
  CORE_REVERSE_ERROR_CODES,
} from "../video-reverse-core/index.js";
import type {
  CoreReverseInput,
  CoreReverseAuditContext,
} from "../video-reverse-core/types.js";
```

注意：
- 保留 AppError 导入（用于错误抛出）
- 保留类型导入（ResolvedRouteProvider, ProviderRouteKey, LlmReverseOutput 等）
- 使用 `import type` 语法导入类型
</action>
  <acceptance_criteria>
    - `grep -n "runCoreReversePipeline" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 1 行
    - `grep -n "createCloneAdapter" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 1 行
    - `grep -n "mapToCloneResult" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 1 行
    - `grep -n "from \"./sync-service.js\"" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行或仅有其他导入（normalizeLlmReverseOutput 导入删除）
    - `grep -n "from \"../../utils/json.js\"" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（extractJsonValue 导入删除）
    - `grep -n "from \"./prompt.js\"" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（buildVideoStoryboardPrompt 导入删除）
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    导入语句已更新，旧导入已移除，TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 重写 runSingleVideoLlmReverse 函数使用核心管道</name>
  <files>src/modules/video-hot-trend/single-reverse-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/single-reverse-service.ts (runSingleVideoLlmReverse function, lines 217-332)
    - src/modules/video-reverse-core/unified-reverse-core.ts (runCoreReversePipeline implementation)
    - src/modules/video-reverse-core/mapper.ts (mapToCloneResult function)
    - src/modules/video-reverse-core/clone-adapter.ts (createCloneAdapter function)
  </read_first>
  <behavior>
    - runSingleVideoLlmReverse 调用 runCoreReversePipeline
    - runSingleVideoLlmReverse 使用 createCloneAdapter(deps)
    - runSingleVideoLlmReverse 使用 mapToCloneResult 映射输出
    - 用户入口错误处理：coreOutput.success === false 时抛出 AppError
    - 删除重复代码：download、LLM dispatch、JSON parse、normalizeLlmReverseOutput
    - 删除 recordRouteAudit 调用（核心管道通过 adapter 处理审计）
  </behavior>
  <action>
    完全重写 runSingleVideoLlmReverse 函数（约 lines 217-332），替换为以下实现：

```typescript
/**
 * 单视频热榜 LLM 反推（使用统一核心管道）
 * 核心管道处理：下载 → base64 → LLM → JSON 解析 → 输出标准化
 * 用户入口错误语义：失败时抛出 AppError（per RESEARCH.md Pattern 3）
 */
export async function runSingleVideoLlmReverse(
  deps: SingleVideoReverseDeps,
  videoUrl: string,
): Promise<SingleVideoReverseResult> {
  // ---- 核心管道调用 ----
  const adapter = createCloneAdapter(deps);
  const coreInput: CoreReverseInput = {
    videoUrl,
    routeKeys: ["hot_trend_labeling", "script_generation"],
    auditContext: {
      routeKey: "reverse_parse",
      businessContext: "视频热榜单视频反推",
    },
  };

  const coreOutput = await runCoreReversePipeline(adapter, coreInput);

  // ---- 用户入口错误处理：失败时抛异常 ----
  // 批量入口（sync-service）返回 failed result，用户入口（single-reverse）抛 AppError
  // per RESEARCH.md Pattern 3: Error Handling Strategy
  if (!coreOutput.success) {
    throw new AppError(
      502,
      coreOutput.errorCode ?? "UNKNOWN",
      coreOutput.errorMessage ?? "反推失败，请稍后重试",
    );
  }

  // ---- 映射输出 ----
  return mapToCloneResult({
    coreOutput,
    videoUrl: coreOutput.resolvedVideoUrl,
    generateId: deps.generateId,
    now: deps.now,
  });
}
```

关键点：
1. createCloneAdapter(deps) 将 SingleVideoReverseDeps 适配为 UnifiedReverseDeps
2. 核心管道处理所有逻辑：resolveVideoUrl、download、provider resolve、LLM call、JSON parse、normalize
3. 用户入口错误语义：抛出 AppError（与批量入口返回 failed result 不同）
4. 删除原有代码：download、provider resolve、LLM dispatch、JSON parse、normalizeLlmReverseOutput、recordRouteAudit
5. adapter 内部处理审计（createLlmDebugRecord/finalize），无需 entry-level 调用
</action>
  <acceptance_criteria>
    - `grep -n "runCoreReversePipeline" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 2 行（import + call）
    - `grep -n "createCloneAdapter" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 2 行（import + call）
    - `grep -n "mapToCloneResult" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 2 行（import + call）
    - `grep -n "throw new AppError" src/modules/video-hot-trend/single-reverse-service.ts` 返回至少 1 行（错误抛出）
    - `grep -n "downloadVideoForLlm" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（重复代码删除）
    - `grep -n "normalizeLlmReverseOutput" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（重复代码删除）
    - `grep -n "recordRouteAudit" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（旧审计调用删除）
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    runSingleVideoLlmReverse 函数已重写，使用核心管道，删除重复代码
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除 mapLlmReverseToResult 函数（由 mapToCloneResult 替代）</name>
  <files>src/modules/video-hot-trend/single-reverse-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/single-reverse-service.ts (mapLlmReverseToResult function, lines 338-502)
    - src/modules/video-reverse-core/mapper.ts (mapToCloneResult implementation)
  </read_first>
  <behavior>
    - mapLlmReverseToResult 函数完全删除（约 lines 338-502）
    - 其功能由 video-reverse-core/mapper.ts 的 mapToCloneResult 替代
    - 函数定义行数减少约 165 行
  </behavior>
  <action>
    删除 mapLlmReverseToResult 函数（约 lines 338-502）。

该函数已被 video-reverse-core/mapper.ts 的 mapToCloneResult 替代：
- Phase 3 已实现 mapToCloneResult，包含相同的转换逻辑
- 无需保留重复的映射代码

删除后，single-reverse-service.ts 文件结构简化：
- 导入区域（约 25 行）
- SingleVideoReverseDeps 接口定义（约 90 行）
- SingleVideoReverseResult 类型定义（约 80 行）
- runSingleVideoLlmReverse 函数（约 30 行）

注意：保留类型定义（SingleVideoReverseResult, SingleVideoReverseResultSection, SingleVideoReverseResultFrame），因为 mapToCloneResult 返回这些类型。
</action>
  <acceptance_criteria>
    - `grep -n "function mapLlmReverseToResult" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（函数删除）
    - `grep -n "mapLlmReverseToResult" src/modules/video-hot-trend/single-reverse-service.ts` 返回 0 行（无引用）
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    mapLlmReverseToResult 函数已删除，由 mapToCloneResult 替代
  </done>
</task>

<task type="auto">
  <name>Task 4: 更新 SingleVideoReverseResult 类型导入来源</name>
  <files>src/modules/video-hot-trend/single-reverse-service.ts</files>
  <read_first>
    - src/modules/video-hot-trend/single-reverse-service.ts (SingleVideoReverseResult type, lines 125-190)
    - src/modules/video-reverse-core/mapper.ts (SingleVideoReverseResult type used in mapToCloneResult)
  </read_first>
  <behavior>
    - SingleVideoReverseResult 类型保留在 single-reverse-service.ts（作为导出类型）
    - mapToCloneResult 返回此类型，类型定义一致
    - rawLlmOutput 字段类型需与 mapper.ts 保持一致（LlmReverseOutput | null）
  </behavior>
  <action>
    检查 SingleVideoReverseResult 类型定义（约 lines 125-190），确保与 mapper.ts 中的类型一致：

关键字段检查：
- rawLlmOutput: 应为 `LlmReverseOutput | null`（允许 null，失败时为 null）
- storyboardPanel: 应允许 null（失败时为 null）
- libraryScript: 应允许 null

修改 rawLlmOutput 字段定义（如果当前不允许 null）：
```typescript
/** 原始 LLM 反推输出（用于持久化到 nrm_script_data） */
rawLlmOutput: LlmReverseOutput | null;  // 允许 null
```

同时修改 storyboardPanel 定义（如果当前不允许 null）：
```typescript
storyboardPanel: {
  ...
} | null;  // 允许 null（失败时）
```

注意：mapper.ts 的 mapToCloneResult 在失败时返回 null 值，类型定义必须兼容。
</action>
  <acceptance_criteria>
    - `grep -n "rawLlmOutput:" src/modules/video-hot-trend/single-reverse-service.ts` 显示 `LlmReverseOutput | null`
    - `grep -n "storyboardPanel:" src/modules/video-hot-trend/single-reverse-service.ts` 显示 `| null`
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    类型定义已更新，与 mapToCloneResult 返回类型兼容
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| single-reverse-service → core pipeline | 依赖注入边界，deps 通过 adapter 传入 |
| core pipeline → LLM provider | 外部服务调用，需 timeout 保护 |
| user input → videoUrl | 外部输入，需验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | videoUrl input | mitigate | Core pipeline resolves URL before download |
| T-04-05 | Denial of Service | LLM call timeout | mitigate | 180_000ms timeout enforced in core pipeline |
| T-04-06 | Information Disclosure | Error message | accept | User-facing error message is sanitized |
</threat_model>

<verification>
- TypeScript 编译通过（npm run build）
- 导入语句正确
- runSingleVideoLlmReverse 调用 runCoreReversePipeline
- 错误处理：失败时抛出 AppError
- mapLlmReverseToResult 函数删除
- 重复代码删除
</verification>

<success_criteria>
- runSingleVideoLlmReverse 使用 runCoreReversePipeline + createCloneAdapter + mapToCloneResult
- 用户入口错误语义：失败时抛出 AppError（502 + errorCode + errorMessage）
- 删除重复代码：download、LLM dispatch、JSON parse、normalizeLlmReverseOutput、recordRouteAudit
- mapLlmReverseToResult 函数删除（由 mapToCloneResult 替代）
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-entry-point-integration/04-02-SUMMARY.md`
</output>