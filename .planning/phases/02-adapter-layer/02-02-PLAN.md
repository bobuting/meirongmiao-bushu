---
phase: 02-adapter-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/video-reverse-core/clone-adapter.ts
autonomous: true
requirements:
  - ADPT-03
user_setup: []
must_haves:
  truths:
    - "CloneAdapter 实现 UnifiedReverseDeps 接口所有方法"
    - "适配器委托给 SingleVideoReverseDeps，不修改现有实现"
    - "callLlm 方法内部封装 Gemini/OpenAI dispatch 逻辑"
    - "extractJsonValue 从 utils/json.ts 导入（SingleDeps 缺少）"
    - "resolveProvider 方法 unwrap { provider, routeKey } 返回 provider"
  artifacts:
    - path: "src/modules/video-reverse-core/clone-adapter.ts"
      provides: "SingleVideoReverseDeps → UnifiedReverseDeps 适配器"
      exports: ["createCloneAdapter"]
      min_lines: 80
  key_links:
    - from: "src/modules/video-reverse-core/clone-adapter.ts"
      to: "src/modules/video-reverse-core/unified-reverse-deps.ts"
      via: "implements UnifiedReverseDeps"
      pattern: "export function createCloneAdapter.*UnifiedReverseDeps"
---

<objective>
创建 CloneAdapter，将 SingleVideoReverseDeps 包装为 UnifiedReverseDeps 接口

Purpose: 实现复刻按钮反推入口的依赖适配，使核心管道可复用
Output: clone-adapter.ts 文件，提供 createCloneAdapter 工厂函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adapter-layer/02-CONTEXT.md
@.planning/phases/02-adapter-layer/02-RESEARCH.md
@.planning/phases/01-core-pipeline-extraction/01-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs - extracted from codebase -->

From src/modules/video-reverse-core/unified-reverse-deps.ts (Target Interface):
```typescript
export interface UnifiedReverseDeps {
  resolveVideoUrl: (inputUrl: string) => Promise<string>;
  downloadVideoForLlm: (url: string) => Promise<{ base64: string; mimeType: string } | null>;
  resolveProvider: (routeKeys: ProviderRouteKey[]) => Promise<VideoHotTrendResolvedProvider | null>;
  callLlm: (
    provider: VideoHotTrendResolvedProvider,
    prompt: string,
    video: { base64: string; mimeType: string },
    timeoutMs: number
  ) => Promise<VideoHotTrendLlmPlainTextResult>;
  createAuditRecord: (input: {
    routeKey: ProviderRouteKey;
    businessContext: string;
    projectId?: string;
    userId?: string;
    messages: Array<{ role: string; content: string }>;
    modelParams: { temperature?: number; max_tokens?: number; top_p?: number };
    provider: VideoHotTrendResolvedProvider;
  }) => { auditId: string; startedAt: number };
  finalizeAuditSuccess: (input: {...}) => void;
  finalizeAuditError: (input: {...}) => void;
  extractJsonValue: (text: string) => unknown | null;
  log: { info: (obj: unknown, msg?: string) => void; warn: (obj: unknown, msg?: string) => void; };
  generateId: () => string;
  now: () => number;
}
```

From src/modules/video-hot-trend/single-reverse-service.ts (Source Interface):
```typescript
export interface SingleVideoReverseDeps {
  downloadVideoForLlm: (sourceUrl: string) => Promise<{ base64: string; mimeType: string } | null>;
  resolveVideoUrl: (inputUrl: string) => Promise<string>;  // SingleDeps HAS this method
  resolveRouteProviderWithFallback: (
    routeKeys: ProviderRouteKey[]
  ) => Promise<{ provider: ResolvedRouteProvider; routeKey: ProviderRouteKey } | null>;
  shouldUseGeminiVideoReverseTransport: (provider: ResolvedRouteProvider) => boolean;
  requestGeminiPlainTextWithVideoPart: (...args) => Promise<LlmPlainTextResult>;
  requestOpenAiPlainTextWithVideoVariants: (...args) => Promise<LlmPlainTextResult>;
  buildGeminiInlineVideoPart: (base64: string, mimeType: string) => Record<string, unknown>;
  buildOpenAiInlineVideoContentVariants: (userPrompt: string, base64: string, mimeType: string) => OpenAiVideoContentVariant[];
  generateId: () => string;  // SingleDeps HAS this method
  now: () => number;
  log: { info: (obj: unknown, msg?: string) => void; warn: (obj: unknown, msg?: string) => void; };
  createLlmDebugRecord: (input: {...}) => { auditId: string; startedAt: number };
  finalizeLlmDebugRecordSuccess: (input: {...}) => void;
  finalizeLlmDebugRecordError: (input: {...}) => void;
  // NOTE: SingleDeps lacks extractJsonValue method - must import from utils/json.ts
}
```

From src/utils/json.ts (Import for extractJsonValue):
```typescript
export function extractJsonValue(text: string): unknown | null;
```

From src/services/llm/gemini-utils.ts (Dispatch Logic):
```typescript
export function shouldUseGeminiVideoReverseTransport(provider: ResolvedRouteProvider): boolean;
export function buildGeminiInlineVideoPart(videoBase64: string, mimeType: string): Record<string, unknown>;
```

From src/services/llm/openai-utils.ts (Dispatch Logic):
```typescript
export function buildOpenAiInlineVideoContentVariants(
  userPrompt: string,
  videoBase64: string,
  mimeType: string
): OpenAiVideoContentVariant[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 CloneAdapter 工厂函数</name>
  <files>src/modules/video-reverse-core/clone-adapter.ts</files>
  <behavior>
    - Test 1: createCloneAdapter 返回的对象满足 UnifiedReverseDeps 接口所有方法
    - Test 2: resolveVideoUrl 直接委托给 SingleDeps.resolveVideoUrl（此入口有此方法）
    - Test 3: resolveProvider 调用 resolveRouteProviderWithFallback 并 unwrap 返回 provider
    - Test 4: callLlm 根据 provider 类型调用 Gemini 或 OpenAI 路径
    - Test 5: generateId 直接委托给 SingleDeps.generateId（此入口有此方法）
    - Test 6: extractJsonValue 从 utils/json.ts 导入（SingleDeps 缺少此方法）
    - Test 7: createAuditRecord 委托给 createLlmDebugRecord（方法名映射）
    - Test 8: finalizeAuditSuccess/Error 委托给 finalizeLlmDebugRecordSuccess/Error
  </behavior>
  <read_first>
    - src/modules/video-reverse-core/unified-reverse-deps.ts
    - src/modules/video-hot-trend/single-reverse-service.ts
    - src/utils/json.ts
    - src/services/llm/gemini-utils.ts
    - src/services/llm/openai-utils.ts
  </read_first>
  <action>
    创建 `src/modules/video-reverse-core/clone-adapter.ts` 文件：

    1. 导入必要的类型和函数：
       ```typescript
       import type { UnifiedReverseDeps } from "./unified-reverse-deps.js";
       import type { SingleVideoReverseDeps } from "../video-hot-trend/single-reverse-service.js";
       import type { VideoHotTrendResolvedProvider, VideoHotTrendLlmPlainTextResult } from "../../contracts/video-hot-trend-sync-contract.js";
       import type { ProviderRouteKey } from "../../contracts/provider-route-policy-contract.js";
       import { extractJsonValue } from "../../utils/json.js";  // SingleDeps lacks this
       import { shouldUseGeminiVideoReverseTransport, buildGeminiInlineVideoPart } from "../../services/llm/gemini-utils.js";
       import { buildOpenAiInlineVideoContentVariants } from "../../services/llm/openai-utils.js";
       ```

    2. 实现 createCloneAdapter 工厂函数，返回 UnifiedReverseDeps 对象：

    **直接委托方法（SingleDeps 已有）：**
    - `resolveVideoUrl`: 直接委托 existingDeps.resolveVideoUrl
    - `downloadVideoForLlm`: 直接委托 existingDeps.downloadVideoForLlm
    - `generateId`: 直接委托 existingDeps.generateId
    - `now`: 直接委托 existingDeps.now
    - `log`: 直接委托 existingDeps.log

    **需要导入的方法（SingleDeps 缺少）：**
    - `extractJsonValue`: 从 `../../utils/json.js` 导入，不委托：
      ```typescript
      extractJsonValue: (text: string) => extractJsonValue(text)
      ```

    **需要转换的方法：**

    - `resolveProvider`: unwrap `{ provider, routeKey }` → `provider`：
      ```typescript
      resolveProvider: async (routeKeys: ProviderRouteKey[]) => {
        const resolved = await existingDeps.resolveRouteProviderWithFallback(routeKeys);
        return resolved?.provider ?? null;
      }
      ```
      注意：需要将 ResolvedRouteProvider 类型适配为 VideoHotTrendResolvedProvider

    - `callLlm`: 内部封装 Gemini/OpenAI dispatch 逻辑：
      ```typescript
      callLlm: async (provider, prompt, video, timeoutMs) => {
        if (shouldUseGeminiVideoReverseTransport(provider)) {
          const videoPart = buildGeminiInlineVideoPart(video.base64, video.mimeType);
          return existingDeps.requestGeminiPlainTextWithVideoPart(
            provider, "", prompt, 0.3, videoPart, { timeoutMsOverride: timeoutMs }
          ) as Promise<VideoHotTrendLlmPlainTextResult>;
        } else {
          const variants = buildOpenAiInlineVideoContentVariants(prompt, video.base64, video.mimeType);
          return existingDeps.requestOpenAiPlainTextWithVideoVariants(
            provider, "", variants, 0.3, { timeoutMsOverride: timeoutMs }
          ) as Promise<VideoHotTrendLlmPlainTextResult>;
        }
      }
      ```
      注意：使用类型断言将 LlmPlainTextResult 转换为 VideoHotTrendLlmPlainTextResult（两者结构相同）

    **方法名映射（审计）：**
    - `createAuditRecord`: 委托给 existingDeps.createLlmDebugRecord
    - `finalizeAuditSuccess`: 委托给 existingDeps.finalizeLlmDebugRecordSuccess
    - `finalizeAuditError`: 委托给 existingDeps.finalizeLlmDebugRecordError

    3. 添加中文注释说明每个方法的转换逻辑
    4. 导出 createCloneAdapter 函数

    **Per D-01（位置）**: 文件位于 src/modules/video-reverse-core/ 目录
    **Per D-02（命名）**: 文件名为 clone-adapter.ts
    **Per D-04（委托）**: 所有方法委托给 existingDeps，不修改现有实现
  </action>
  <acceptance_criteria>
    - 文件存在：`grep -l "createCloneAdapter" src/modules/video-reverse-core/clone-adapter.ts`
    - 导入 UnifiedReverseDeps：`grep "import.*UnifiedReverseDeps" src/modules/video-reverse-core/clone-adapter.ts`
    - 导入 SingleVideoReverseDeps：`grep "import.*SingleVideoReverseDeps" src/modules/video-reverse-core/clone-adapter.ts`
    - 导入 extractJsonValue from utils/json：`grep "import.*extractJsonValue.*utils/json" src/modules/video-reverse-core/clone-adapter.ts`
    - 实现 resolveProvider unwrap：`grep "resolved?.provider.*null" src/modules/video-reverse-core/clone-adapter.ts`
    - 实现 callLlm dispatch：`grep "shouldUseGeminiVideoReverseTransport.*provider" src/modules/video-reverse-core/clone-adapter.ts`
    - 实现 extractJsonValue 导入：`grep "extractJsonValue.*extractJsonValue" src/modules/video-reverse-core/clone-adapter.ts`
    - 导出工厂函数：`grep "export function createCloneAdapter" src/modules/video-reverse-core/clone-adapter.ts`
    - TypeScript 编译通过：无错误
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/modules/video-reverse-core/clone-adapter.ts 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    clone-adapter.ts 文件存在，实现所有 11 个 UnifiedReverseDeps 方法，
    TypeScript 类型检查通过，适配器正确委托给 SingleVideoReverseDeps
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新核心模块公共导出</name>
  <files>src/modules/video-reverse-core/index.ts</files>
  <read_first>
    - src/modules/video-reverse-core/index.ts
    - src/modules/video-reverse-core/clone-adapter.ts
  </read_first>
  <action>
    更新 `src/modules/video-reverse-core/index.ts`，添加 CloneAdapter 导出：

    1. 添加 export 语句：
       ```typescript
       // 适配器
       export { createBatchReverseAdapter } from "./batch-reverse-adapter.js";
       export { createCloneAdapter } from "./clone-adapter.js";
       ```

    2. 保持现有导出不变（runCoreReversePipeline, normalizeLlmReverseOutput 等）

    **注意**: 使用 `.js` 后缀以符合 ESM 模块规范
  </action>
  <acceptance_criteria>
    - 导出 createCloneAdapter：`grep "export.*createCloneAdapter" src/modules/video-reverse-core/index.ts`
  </acceptance_criteria>
  <verify>
    <automated>grep "export.*createCloneAdapter" src/modules/video-reverse-core/index.ts</automated>
  </verify>
  <done>
    index.ts 导出 createCloneAdapter，适配器可从核心模块公共访问
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → Deps | 适配器委托给现有 deps，无新信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | callLlm dispatch | mitigate | TypeScript strict type checking enforces correct method signatures |
| T-02-02-02 | Tampering | extractJsonValue import | accept | Import from verified utils/json.ts, no runtime validation needed |
| T-02-02-03 | Tampering | resolveProvider unwrap | accept | Unwrap logic is straightforward, no complex validation needed |
</threat_model>

<verification>
1. TypeScript 编译无错误
2. 适配器实现所有 UnifiedReverseDeps 接口方法
3. extractJsonValue 正确从 utils/json.ts 导入
4. 方法委托逻辑正确（直接委托、unwrap、dispatch）
</verification>

<success_criteria>
- [ ] clone-adapter.ts 文件存在
- [ ] createCloneAdapter 函数导出
- [ ] 所有 11 个 UnifiedReverseDeps 方法实现
- [ ] resolveVideoUrl 直接委托（SingleDeps 有此方法）
- [ ] resolveProvider 实现 unwrap 逻辑
- [ ] callLlm 实现 Gemini/OpenAI dispatch
- [ ] generateId 直接委托（SingleDeps 有此方法）
- [ ] extractJsonValue 从 utils/json.ts 导入
- [ ] 审计方法正确映射（createAuditRecord → createLlmDebugRecord）
- [ ] index.ts 导出 createCloneAdapter
- [ ] TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-adapter-layer/02-02-SUMMARY.md`
</output>