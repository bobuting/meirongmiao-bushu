---
phase: 02-adapter-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/video-reverse-core/batch-reverse-adapter.ts
autonomous: true
requirements:
  - ADPT-01
user_setup: []
must_haves:
  truths:
    - "BatchSyncAdapter 实现 UnifiedReverseDeps 接口所有方法"
    - "适配器委托给 VideoHotTrendSyncDeps，不修改现有实现"
    - "callLlm 方法内部封装 Gemini/OpenAI dispatch 逻辑"
    - "generateId 方法提供独立实现（deps.now() + 随机字符串）"
    - "resolveProvider 方法 unwrap { provider, routeKey } 返回 provider"
  artifacts:
    - path: "src/modules/video-reverse-core/batch-reverse-adapter.ts"
      provides: "VideoHotTrendSyncDeps → UnifiedReverseDeps 适配器"
      exports: ["createBatchReverseAdapter"]
      min_lines: 80
  key_links:
    - from: "src/modules/video-reverse-core/batch-reverse-adapter.ts"
      to: "src/modules/video-reverse-core/unified-reverse-deps.ts"
      via: "implements UnifiedReverseDeps"
      pattern: "export function createBatchReverseAdapter.*UnifiedReverseDeps"
---

<objective>
创建 BatchSyncAdapter，将 VideoHotTrendSyncDeps 包装为 UnifiedReverseDeps 接口

Purpose: 实现批量热榜反推入口的依赖适配，使核心管道可复用
Output: batch-reverse-adapter.ts 文件，提供 createBatchReverseAdapter 工厂函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adapter-layer/02-CONTEXT.md
@.planning/phases/02-adapter-layer/02-RESEARCH.md
@.planning/phases/01-core-pipeline-extraction/01-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs - extracted from codebase -->

From src/modules/video-reverse-core/unified-reverse-deps.ts (Target Interface):
```typescript
export interface UnifiedReverseDeps {
  resolveVideoUrl: (inputUrl: string) => Promise<string>;
  downloadVideoForLlm: (url: string) => Promise<{ base64: string; mimeType: string } | null>;
  resolveProvider: (routeKeys: ProviderRouteKey[]) => Promise<VideoHotTrendResolvedProvider | null>;
  callLlm: (
    provider: VideoHotTrendResolvedProvider,
    prompt: string,
    video: { base64: string; mimeType: string },
    timeoutMs: number
  ) => Promise<VideoHotTrendLlmPlainTextResult>;
  createAuditRecord: (input: {
    routeKey: ProviderRouteKey;
    businessContext: string;
    projectId?: string;
    userId?: string;
    messages: Array<{ role: string; content: string }>;
    modelParams: { temperature?: number; max_tokens?: number; top_p?: number };
    provider: VideoHotTrendResolvedProvider;
  }) => { auditId: string; startedAt: number };
  finalizeAuditSuccess: (input: {
    auditId: string;
    startedAt: number;
    actualModel: string;
    responseText: string;
    inputTokens?: number;
    outputTokens?: number;
    ttftMs?: number;
  }) => void;
  finalizeAuditError: (input: {
    auditId: string;
    startedAt: number;
    errorCode: string;
    errorMessage: string;
  }) => void;
  extractJsonValue: (text: string) => unknown | null;
  log: { info: (obj: unknown, msg?: string) => void; warn: (obj: unknown, msg?: string) => void; };
  generateId: () => string;
  now: () => number;
}
```

From src/contracts/video-hot-trend-sync-contract.ts (Source Interface):
```typescript
export interface VideoHotTrendSyncDeps {
  resolveRouteProviderWithFallback: (
    routeKeys: ProviderRouteKey[]
  ) => Promise<{ provider: VideoHotTrendResolvedProvider; routeKey: ProviderRouteKey } | null>;
  requestGeminiPlainTextWithVideoPart: (
    provider: VideoHotTrendResolvedProvider,
    systemPrompt: string,
    userPrompt: string,
    temperature: number,
    videoPart: Record<string, unknown>,
    requestOptions?: { timeoutMsOverride?: number }
  ) => Promise<VideoHotTrendLlmPlainTextResult>;
  requestOpenAiPlainTextWithVideoVariants: (
    provider: VideoHotTrendResolvedProvider,
    systemPrompt: string,
    variants: VideoHotTrendOpenAiVideoContentVariant[],
    temperature: number,
    requestOptions?: { timeoutMsOverride?: number }
  ) => Promise<VideoHotTrendLlmPlainTextResult>;
  downloadVideoForLlm: (sourceUrl: string) => Promise<{ base64: string; mimeType: string } | null>;
  createLlmDebugRecord: (input: {...}) => { auditId: string; startedAt: number };
  finalizeLlmDebugRecordSuccess: (input: {...}) => void;
  finalizeLlmDebugRecordError: (input: {...}) => void;
  extractJsonValue: (text: string) => unknown | null;
  log: { info: (obj: Record<string, unknown>, msg: string) => void; warn: (obj: Record<string, unknown>, msg: string) => void; };
  now: () => number;
  // NOTE: VideoHotTrendSyncDeps lacks resolveVideoUrl and generateId methods
}
```

From src/services/llm/gemini-utils.ts (Dispatch Logic):
```typescript
export function shouldUseGeminiVideoReverseTransport(provider: ResolvedRouteProvider): boolean;
export function buildGeminiInlineVideoPart(videoBase64: string, mimeType: string): Record<string, unknown>;
```

From src/services/llm/openai-utils.ts (Dispatch Logic):
```typescript
export function buildOpenAiInlineVideoContentVariants(
  userPrompt: string,
  videoBase64: string,
  mimeType: string
): OpenAiVideoContentVariant[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 BatchSyncAdapter 工厂函数</name>
  <files>src/modules/video-reverse-core/batch-reverse-adapter.ts</files>
  <behavior>
    - Test 1: createBatchReverseAdapter 返回的对象满足 UnifiedReverseDeps 接口所有方法
    - Test 2: resolveVideoUrl 返回输入 URL（identity function，因为 SyncDeps 缺少此方法）
    - Test 3: resolveProvider 调用 resolveRouteProviderWithFallback 并 unwrap 返回 provider
    - Test 4: callLlm 根据 provider 类型调用 Gemini 或 OpenAI 路径
    - Test 5: generateId 使用 now() + 随机字符串生成唯一 ID
    - Test 6: createAuditRecord 委托给 createLlmDebugRecord（方法名映射）
    - Test 7: finalizeAuditSuccess/Error 委托给 finalizeLlmDebugRecordSuccess/Error
  </behavior>
  <read_first>
    - src/modules/video-reverse-core/unified-reverse-deps.ts
    - src/contracts/video-hot-trend-sync-contract.ts
    - src/services/llm/gemini-utils.ts
    - src/services/llm/openai-utils.ts
  </read_first>
  <action>
    创建 `src/modules/video-reverse-core/batch-reverse-adapter.ts` 文件：

    1. 导入必要的类型和函数：
       ```typescript
       import type { UnifiedReverseDeps } from "./unified-reverse-deps.js";
       import type { VideoHotTrendSyncDeps } from "../../contracts/video-hot-trend-sync-contract.js";
       import { shouldUseGeminiVideoReverseTransport, buildGeminiInlineVideoPart } from "../../services/llm/gemini-utils.js";
       import { buildOpenAiInlineVideoContentVariants } from "../../services/llm/openai-utils.js";
       ```

    2. 实现 createBatchReverseAdapter 工厂函数，返回 UnifiedReverseDeps 对象：

    **直接委托方法：**
    - `downloadVideoForLlm`: 直接委托 existingDeps.downloadVideoForLlm
    - `extractJsonValue`: 直接委托 existingDeps.extractJsonValue
    - `log`: 直接委托 existingDeps.log
    - `now`: 直接委托 existingDeps.now

    **需要转换的方法：**

    - `resolveVideoUrl`: SyncDeps 缺少此方法，实现为 identity function：
      ```typescript
      resolveVideoUrl: async (inputUrl: string) => inputUrl
      ```

    - `resolveProvider`: unwrap `{ provider, routeKey }` → `provider`：
      ```typescript
      resolveProvider: async (routeKeys) => {
        const resolved = await existingDeps.resolveRouteProviderWithFallback(routeKeys);
        return resolved?.provider ?? null;
      }
      ```

    - `callLlm`: 内部封装 Gemini/OpenAI dispatch 逻辑：
      ```typescript
      callLlm: async (provider, prompt, video, timeoutMs) => {
        if (shouldUseGeminiVideoReverseTransport(provider)) {
          const videoPart = buildGeminiInlineVideoPart(video.base64, video.mimeType);
          return existingDeps.requestGeminiPlainTextWithVideoPart(
            provider, "", prompt, 0.3, videoPart, { timeoutMsOverride: timeoutMs }
          );
        } else {
          const variants = buildOpenAiInlineVideoContentVariants(prompt, video.base64, video.mimeType);
          return existingDeps.requestOpenAiPlainTextWithVideoVariants(
            provider, "", variants, 0.3, { timeoutMsOverride: timeoutMs }
          );
        }
      }
      ```

    **方法名映射（审计）：**
    - `createAuditRecord`: 委托给 existingDeps.createLlmDebugRecord
    - `finalizeAuditSuccess`: 委托给 existingDeps.finalizeLlmDebugRecordSuccess
    - `finalizeAuditError`: 委托给 existingDeps.finalizeLlmDebugRecordError

    **需要提供的方法：**
    - `generateId`: SyncDeps 缺少此方法，使用 now() + 随机字符串：
      ```typescript
      generateId: () => `batch-${existingDeps.now()}-${Math.random().toString(36).slice(2, 11)}`
      ```

    3. 添加中文注释说明每个方法的转换逻辑
    4. 导出 createBatchReverseAdapter 函数

    **Per D-01（位置）**: 文件位于 src/modules/video-reverse-core/ 目录
    **Per D-02（命名）**: 文件名为 batch-reverse-adapter.ts
    **Per D-04（委托）**: 所有方法委托给 existingDeps，不修改现有实现
  </action>
  <acceptance_criteria>
    - 文件存在：`grep -l "createBatchReverseAdapter" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 导入 UnifiedReverseDeps：`grep "import.*UnifiedReverseDeps" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 导入 VideoHotTrendSyncDeps：`grep "import.*VideoHotTrendSyncDeps" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 实现 resolveVideoUrl：`grep "resolveVideoUrl.*async.*inputUrl" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 实现 resolveProvider unwrap：`grep "resolved?.provider.*null" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 实现 callLlm dispatch：`grep "shouldUseGeminiVideoReverseTransport.*provider" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 实现 generateId：`grep "generateId.*batch.*now.*random" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - 导出工厂函数：`grep "export function createBatchReverseAdapter" src/modules/video-reverse-core/batch-reverse-adapter.ts`
    - TypeScript 编译通过：无错误
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/modules/video-reverse-core/batch-reverse-adapter.ts 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    batch-reverse-adapter.ts 文件存在，实现所有 11 个 UnifiedReverseDeps 方法，
    TypeScript 类型检查通过，适配器正确委托给 VideoHotTrendSyncDeps
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新核心模块公共导出</name>
  <files>src/modules/video-reverse-core/index.ts</files>
  <read_first>
    - src/modules/video-reverse-core/index.ts
    - src/modules/video-reverse-core/batch-reverse-adapter.ts
  </read_first>
  <action>
    更新 `src/modules/video-reverse-core/index.ts`，添加 BatchSyncAdapter 导出：

    1. 添加 export 语句：
       ```typescript
       // 适配器
       export { createBatchReverseAdapter } from "./batch-reverse-adapter.js";
       ```

    2. 保持现有导出不变（runCoreReversePipeline, normalizeLlmReverseOutput 等）

    **注意**: 使用 `.js` 后缀以符合 ESM 模块规范
  </action>
  <acceptance_criteria>
    - 导出 createBatchReverseAdapter：`grep "export.*createBatchReverseAdapter" src/modules/video-reverse-core/index.ts`
  </acceptance_criteria>
  <verify>
    <automated>grep "export.*createBatchReverseAdapter" src/modules/video-reverse-core/index.ts</automated>
  </verify>
  <done>
    index.ts 导出 createBatchReverseAdapter，适配器可从核心模块公共访问
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → Deps | 适配器委托给现有 deps，无新信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | callLlm dispatch | mitigate | TypeScript strict type checking enforces correct method signatures |
| T-02-01-02 | Spoofing | generateId uniqueness | mitigate | Use now() timestamp + random string ensures uniqueness |
| T-02-01-03 | Tampering | resolveProvider unwrap | accept | Unwrap logic is straightforward, no complex validation needed |
</threat_model>

<verification>
1. TypeScript 编译无错误
2. 适配器实现所有 UnifiedReverseDeps 接口方法
3. 方法委托逻辑正确（直接委托、unwrap、dispatch）
</verification>

<success_criteria>
- [ ] batch-reverse-adapter.ts 文件存在
- [ ] createBatchReverseAdapter 函数导出
- [ ] 所有 11 个 UnifiedReverseDeps 方法实现
- [ ] resolveVideoUrl 实现 identity function
- [ ] resolveProvider 实现 unwrap 逻辑
- [ ] callLlm 实现 Gemini/OpenAI dispatch
- [ ] generateId 使用 now() + random 实现
- [ ] 审计方法正确映射（createAuditRecord → createLlmDebugRecord）
- [ ] index.ts 导出 createBatchReverseAdapter
- [ ] TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-adapter-layer/02-01-SUMMARY.md`
</output>