---
phase: 04-step4-ecommerce-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/types.ts
  - src/contracts/repository-ports/library-repository.ts
  - src/repositories/pg/page-section-pg-repository.ts
  - src/repositories/pg/section-version-pg-repository.ts
  - src/repositories/pg/index.ts
  - src/modules/section-planning-service.ts
  - src/modules/section-generation-service.ts
  - src/routes/image-project/step4-handlers.ts
  - src/routes/image-project/index.ts
autonomous: true
requirements:
  - IMG-04
must_haves:
  truths:
    - "PageSection 和 SectionVersion 类型定义已存在于 src/contracts/types.ts"
    - "IPageSectionRepository 和 ISectionVersionRepository 接口已定义并注册"
    - "AI 规划接口可调用，返回 section 列表（type、title、goal、copy、visualPrompt）"
    - "每个 section 可独立生成/重新生成图片，失败时直接报错（无 SVG 降级）"
    - "每次生成自动创建 SectionVersion 记录"
    - "Section CRUD API 可正常查询、创建、更新、删除"
  artifacts:
    - path: "src/contracts/types.ts"
      provides: "PageSection, SectionVersion, SectionType, SectionStatus 接口"
      contains: "export type SectionType ="
    - path: "src/contracts/repository-ports/library-repository.ts"
      provides: "IPageSectionRepository, ISectionVersionRepository 接口"
      exports: ["IPageSectionRepository", "ISectionVersionRepository"]
    - path: "src/repositories/pg/page-section-pg-repository.ts"
      provides: "PgPageSectionRepository 实现"
      exports: ["PgPageSectionRepository"]
    - path: "src/repositories/pg/section-version-pg-repository.ts"
      provides: "PgSectionVersionRepository 实现"
      exports: ["PgSectionVersionRepository"]
    - path: "src/modules/section-planning-service.ts"
      provides: "SectionPlanningService — AI 规划页面结构"
      exports: ["SectionPlanningService"]
    - path: "src/modules/section-generation-service.ts"
      provides: "SectionGenerationService — 图片生成 + 版本管理"
      exports: ["SectionGenerationService"]
    - path: "src/routes/image-project/step4-handlers.ts"
      provides: "Step 4 所有路由 handlers"
      exports: ["registerImageProjectStep4Routes"]
  key_links:
    - from: "src/modules/section-planning-service.ts"
      to: "src/services/llm/llm-transport.ts"
      via: "requestLlmPlainText"
      pattern: "requestLlmPlainText"
    - from: "src/modules/section-generation-service.ts"
      to: "src/services/media/image-generation-providers.ts"
      via: "requestJimengImageUrl"
      pattern: "requestJimengImageUrl"
    - from: "src/modules/section-generation-service.ts"
      to: "src/services/media/storage-persist.ts"
      via: "persistImageSourceToStorage"
      pattern: "persistImageSourceToStorage"
    - from: "src/routes/image-project/step4-handlers.ts"
      to: "src/modules/section-planning-service.ts"
      via: "ctx.repos.pageSections"
      pattern: "pageSections"
threat_model:
  Trust Boundaries:
    - Boundary: "client→API (projectId auth)"
      Description: "projectId 参数来自客户端，需验证用户所有权"
    - Boundary: "LLM→planning service"
      Description: "LLM 返回不可信 JSON，需严格校验 section type 和字段"
    - Boundary: "API→image generation"
      Description: "图片生成 Provider 返回的 URL 需持久化到对象存储"

  STRIDE Threat Register:
    | Threat ID | Category | Component | Disposition | Mitigation Plan |
    | T-04-01 | Spoofing | step4-handlers (all) | mitigate | 所有 handler 入口调用 requireUser() + requireOwnerProject() 验证项目所有权 |
    | T-04-02 | Tampering | section-planning-service (LLM JSON parsing) | mitigate | 使用 isValidSectionPlanItem() 严格校验每个字段类型和长度，reject 无效条目 |
    | T-04-03 | Information Disclosure | page-section-pg-repository (findByProjectId) | mitigate | 所有查询通过 projectId 过滤，不暴露其他用户数据 |
    | T-04-04 | Denial of Service | section-generation-service (image generation) | mitigate | 逐张生成，单张失败不阻塞其余 section；生成状态用 status 字段标记 |
    | T-04-05 | Injection | step4-handlers (section CRUD) | accept | 所有参数通过 pg 参数化查询 ($1, $2)，无 SQL 拼接风险 |
---

<objective>
实现 Step 4 电商详情页的完整后端：类型定义、Repository 层、AI 规划服务、图片生成服务、路由 handlers。

Purpose: 为前端三栏编辑器提供可靠的 Section CRUD、AI 页面结构规划、分区块图片生成和版本管理能力。
Output: 7 个新文件 + 3 个修改文件，所有路由注册到 /image-projects/:projectId/step4/ 路径下。
</objective>

<execution_context>
@.planning/get-shit-done/workflows/execute-plan.md
@.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-step4-ecommerce-page/04-CONTEXT.md

# 类型参考
@src/contracts/types.ts — ModelPhoto 接口（参考结构，L1064-1078）
@src/contracts/project-step-snapshot.ts — ImageStep4Snapshot（L79-85）

# Repository 参考
@src/repositories/pg/model-photo-pg-repository.ts — PgModelPhotoRepository 实现模式
@src/repositories/pg/index.ts — PgRepositoryCollection 注册模式
@src/repositories/pg/base-pg-repository.ts — PgBaseRepository 基类

# 服务参考
@src/services/media/image-generation-providers.ts — requestJimengImageUrl
@src/services/media/storage-persist.ts — persistImageSourceToStorage
@src/modules/prompt/prompt-helper.ts — getPromptContent
@src/services/llm/provider-resolver.ts — resolveRouteProviderWithFallback
@src/services/llm/llm-transport.ts — requestLlmPlainText

# 路由参考
@src/routes/image-project/step3-handlers.ts — Step 3 路由模式（LLM 规划 + 逐张生成）
@src/routes/image-project/index.ts — 图片项目路由注册入口
@src/core/errors.ts — AppError 类

# banana-mall 参考
@/tmp/banana-mall/types/domain.ts — sectionTypes 枚举（11 种）
@/tmp/banana-mall/lib/ai/schemas/section-plan.ts — Section Plan Schema
@/tmp/banana-mall/lib/services/planner-service.ts — 规划服务参考
@/tmp/banana-mall/lib/services/generation-service.ts — 生成服务参考

# 架构约束
- app.ts 只减不增，路由放 src/routes/image-project/
- 提示词通过提示词管理模块，禁止硬编码
- 主流程失败直接报错，禁止静默降级（包括 SVG 降级 — 不做）
- 不创建数据库迁移文件，直接操作数据库
- 单文件不超过 1200 行，超过需拆分
- TypeScript 严禁 any
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/contracts/types.ts (ModelPhoto — 参考结构):
```typescript
export type ModelPhotoStatus = 'pending' | 'generating' | 'success' | 'failed';
export interface ModelPhoto {
  id: string;
  projectId: string;
  imageUrl: string | null;
  poseLabel: string;
  bgLabel: string;
  isSelected: boolean;
  status: ModelPhotoStatus;
  errorMessage: string | null;
  order: number;
  createdAt: number;
  updatedAt: number;
}
```

From src/routes/image-project/step3-handlers.ts (LLM 规划模式):
```typescript
// LLM 规划 → extractJsonObject → 校验 → 逐张生成
const planItems = (planJson.photos as unknown[]).filter(isValidPlanItem);
const clampedPlan = clampPlanCount(planItems);
// 逐张生成，失败 catch 后标记 status='failed'
```

From src/modules/prompt/prompt-helper.ts:
```typescript
export interface PromptResult {
  systemPrompt: string;
  userPrompt: string;
  prompt: string;
  templateId: string;
  version: number;
}
export function getPromptContent(promptCode: string, variables: Record<string, string>): Promise<PromptResult>;
```

From src/repositories/pg/model-photo-pg-repository.ts (Repository 模式):
```typescript
export class PgModelPhotoRepository extends PgBaseRepository<ModelPhoto> implements IModelPhotoRepository {
  constructor(pool: Pool, client?: PoolClient);
  protected mapRow(row: Record<string, unknown>): ModelPhoto;
  protected mapEntity(v: ModelPhoto): Record<string, unknown>;
  async findByProjectId(projectId: string): Promise<ModelPhoto[]>;
  async bulkCreate(photos: ModelPhoto[]): Promise<void>;
}
```

From src/repositories/pg/index.ts (PgRepositoryCollection):
```typescript
export interface PgRepositoryCollection {
  modelPhotos: PgModelPhotoRepository;
  // ... 其他 repos
  withTransaction: <T>(fn: (txRepos: PgRepositoryCollection) => Promise<T>) => Promise<T>;
}
```

From src/routes/image-project/index.ts (路由注册):
```typescript
export function registerImageProjectRoutes(app: FastifyInstance, ctx: AppContext): void {
  registerImageProjectStep1Routes(app, ctx);
  registerImageProjectStep2Routes(app, ctx);
  registerImageProjectStep3Routes(app, ctx);
  // 需要新增: registerImageProjectStep4Routes(app, ctx);
}
```

From src/services/media/image-generation-providers.ts:
```typescript
export async function requestJimengImageUrl(
  provider: string,
  prompt: string,
  options?: { mode?: "text_to_image" | "image_to_image"; images?: string[] }
): Promise<string>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 类型定义 + Repository 层</name>
  <files>src/contracts/types.ts, src/contracts/repository-ports/library-repository.ts, src/repositories/pg/page-section-pg-repository.ts, src/repositories/pg/section-version-pg-repository.ts, src/repositories/pg/index.ts</files>
  <action>
1. 在 `src/contracts/types.ts` 末尾（ModelPhoto 接口后）添加以下类型：

```typescript
// ============================================================================
// 电商详情页 Section（图片项目 Step 4）
// ============================================================================

/** 页面区块类型（11 种，完整复用 banana-mall） */
export type SectionType =
  | 'hero'           // 头图主视觉
  | 'selling_points' // 卖点模块
  | 'scenario'       // 场景展示
  | 'detail_closeup' // 细节特写
  | 'specs'          // 规格参数
  | 'material'       // 材质工艺
  | 'comparison'     // 对比说明
  | 'gift_scene'     // 送礼场景
  | 'brand_trust'    // 品牌信任
  | 'summary'        // 总结收口
  | 'custom';        // 自定义模块

/** 页面区块生成状态 */
export type SectionStatus = 'idle' | 'generating' | 'success' | 'failed';

/** 页面区块（nrm_page_sections） */
export interface PageSection {
  id: string;
  projectId: string;
  sectionKey: string;           // AI 规划的唯一标识，如 "hero_0", "selling_0"
  sectionType: SectionType;
  title: string;                // 区块标题
  goal: string;                 // 区块目标（AI 生成）
  copy: string;                 // 区块文案（AI 生成）
  visualPrompt: string;         // 视觉生成提示词
  sortOrder: number;            // 排序序号
  status: SectionStatus;
  currentImageUrl: string | null; // 当前激活版本的图片 URL
  currentVersionNumber: number;   // 当前版本号（从 1 开始）
  editableData: Record<string, unknown> | null; // 用户可编辑数据
  errorMessage: string | null;
  createdAt: number;
  updatedAt: number;
}

/** 区块版本历史（nrm_section_versions） */
export interface SectionVersion {
  id: string;
  sectionId: string;
  projectId: string;
  versionNumber: number;
  imageUrl: string | null;
  promptSnapshot: Record<string, unknown> | null;
  copySnapshot: Record<string, unknown> | null;
  isActive: boolean;
  createdAt: number;
}
```

2. 在 `src/contracts/repository-ports/library-repository.ts` 中添加接口（参考 IModelPhotoRepository 模式）：

```typescript
import type { PageSection, SectionVersion } from "../types.js";
import type { IRepositoryClock } from "./common.js";

export interface IPageSectionRepository {
  findByProjectId(projectId: string): Promise<PageSection[]>;
  findById(id: string): Promise<PageSection | null>;
  create(section: PageSection): Promise<void>;
  bulkCreate(sections: PageSection[]): Promise<void>;
  updateFields(id: string, fields: Partial<PageSection>): Promise<void>;
  deleteById(id: string): Promise<void>;
  deleteByProjectId(projectId: string): Promise<void>;
  updateOrder(sectionIds: string[]): Promise<void>;
}

export interface ISectionVersionRepository {
  findBySectionId(sectionId: string): Promise<SectionVersion[]>;
  findActiveBySectionId(sectionId: string): Promise<SectionVersion | null>;
  create(version: SectionVersion): Promise<void>;
  activateVersion(id: string): Promise<void>;
  deleteBySectionId(sectionId: string): Promise<void>;
}
```

3. 创建 `src/repositories/pg/page-section-pg-repository.ts`：
   - 继承 PgBaseRepository<PageSection>，实现 IPageSectionRepository
   - 表名：nrm("page_sections")
   - mapRow: snake_case → camelCase（project_id→projectId, section_key→sectionKey, section_type→sectionType, sort_order→sortOrder, status→status, current_image_url→currentImageUrl, current_version_number→currentVersionNumber, editable_data→editableData, error_message→errorMessage）
   - mapEntity: camelCase → snake_case
   - findByProjectId: WHERE project_id = $1 ORDER BY sort_order ASC
   - bulkCreate: 批量 INSERT（参考 PgModelPhotoRepository 的 bulkCreate 模式）
   - updateOrder: UPDATE nrm_page_sections SET sort_order = CASE WHEN id=$1 THEN 1 WHEN id=$2 THEN 2 END WHERE id IN (...)
   - updateFields: UPDATE SET 动态字段

4. 创建 `src/repositories/pg/section-version-pg-repository.ts`：
   - 继承 PgBaseRepository<SectionVersion>，实现 ISectionVersionRepository
   - 表名：nrm("section_versions")
   - mapRow: snake_case → camelCase（section_id→sectionId, version_number→versionNumber, image_url→imageUrl, prompt_snapshot→promptSnapshot, copy_snapshot→copySnapshot, is_active→isActive）
   - findBySectionId: WHERE section_id = $1 ORDER BY version_number DESC
   - findActiveBySectionId: WHERE section_id = $1 AND is_active = true LIMIT 1
   - activateVersion: 先 SET is_active=false WHERE section_id=X，再 SET is_active=true WHERE id=Y

5. 在 `src/repositories/pg/index.ts` 中注册新 Repository：
   - import 新增的两个 Repository 类
   - 在 PgRepositoryCollection 接口中添加 pageSections 和 sectionVersions 字段
   - 在 createPgRepositories 函数中实例化
   - 在 createPgRepositoriesFromClient 函数中实例化（事务支持）

**注意：** nrm_page_sections 和 nrm_section_versions 表在 Phase 1 已创建。如果表不存在，执行以下 SQL 创建：

```sql
CREATE TABLE IF NOT EXISTS nrm_page_sections (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  section_key TEXT NOT NULL,
  section_type TEXT NOT NULL,
  title TEXT NOT NULL DEFAULT '',
  goal TEXT NOT NULL DEFAULT '',
  copy TEXT NOT NULL DEFAULT '',
  visual_prompt TEXT NOT NULL DEFAULT '',
  sort_order INTEGER DEFAULT 0,
  status TEXT DEFAULT 'idle',
  current_image_url TEXT,
  current_version_number INTEGER DEFAULT 0,
  editable_data JSONB,
  error_message TEXT,
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_sections_project_key ON nrm_page_sections(project_id, section_key);
CREATE INDEX IF NOT EXISTS idx_sections_project ON nrm_page_sections(project_id);

CREATE TABLE IF NOT EXISTS nrm_section_versions (
  id TEXT PRIMARY KEY,
  section_id TEXT NOT NULL,
  project_id TEXT NOT NULL,
  version_number INTEGER NOT NULL,
  image_url TEXT,
  prompt_snapshot JSONB,
  copy_snapshot JSONB,
  is_active BOOLEAN DEFAULT false,
  created_at BIGINT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_versions_section ON nrm_section_versions(section_id);
CREATE INDEX IF NOT EXISTS idx_versions_project ON nrm_section_versions(project_id);
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PageSection 和 SectionVersion 类型在 types.ts 中；IPageSectionRepository 和 ISectionVersionRepository 接口在 library-repository.ts 中；两个 Repository 实现文件存在且 TypeScript 编译通过；PgRepositoryCollection 包含 pageSections 和 sectionVersions。</done>
</task>

<task type="auto">
  <name>Task 2: Section 规划服务（AI 页面结构规划）</name>
  <files>src/modules/section-planning-service.ts</files>
  <action>
创建 `src/modules/section-planning-service.ts`，实现 AI 驱动的页面结构规划服务。

**Prompt code:** `step4_section_planning`（需在提示词管理模块中配置，若不存在则使用内联 fallback）

**核心方法：** `planSections(ctx, projectId) → Promise<{ sections: PageSection[] }>`

实现步骤：

1. 定义 SectionPlanItem 接口：
```typescript
interface SectionPlanItem {
  sectionKey: string;   // 如 "hero_0", "selling_0"
  type: SectionType;
  title: string;
  goal: string;
  copy: string;
  visualPrompt: string;
}
```

2. 定义 isValidSectionPlanItem() 校验函数：
   - 校验 type 必须是 11 种 SectionType 之一
   - sectionKey, title, goal, copy, visualPrompt 都必须是字符串且非空

3. 定义 clampSectionCount() 函数：
   - 限制 section 数量在 5-15 个之间（太少信息不足，太多性能差）
   - 优先保留 hero 和 summary 类型

4. 定义 buildSectionPlanPrompt() 函数：
   - 收集搭配方案摘要（outfitPlan.summary 或 body.outfitSummary）
   - 收集角色描述（body.characterDescription）
   - 收集模特图信息（poseLabel + bgLabel 列表）
   - 返回格式化的中文 prompt，要求 LLM 输出 JSON 数组

5. 实现 planSections() 主方法：
   - 通过 ctx.repos.projects.findById 获取项目
   - 通过 ctx.repos.outfitPlans.findById 获取搭配方案
   - 通过 ctx.repos.modelPhotos.findByProjectId 获取模特图列表
   - 构建 prompt，通过 getPromptContent('step4_section_planning', { userPrompt }) 获取提示词
   - 通过 resolveRouteProviderWithFallback(ctx, ['script_generation']) 获取 LLM Provider
   - 调用 requestLlmPlainText 获取 LLM 响应
   - 通过 extractJsonObject 解析 JSON
   - 校验并过滤有效条目
   - 返回 section 列表（不落库，只返回规划结果）

6. **错误处理：** 遵循项目规范，主流程失败直接报错
   - LLM Provider 未配置 → AppError(500, 'PROVIDER_NOT_CONFIGURED')
   - LLM 返回无效 JSON → AppError(400, 'INVALID_JSON', 'AI 返回的页面规划格式不正确')
   - 无有效条目 → AppError(400, 'INVALID_JSON', 'AI 返回的规划方案中没有有效的区块')

**注意：** 提示词必须通过 getPromptContent 获取，不要硬编码。Prompt code 为 `step4_section_planning`。如果提示词管理系统中还没有这个 code，先在提示词管理模块中创建（通过数据库或管理后台），代码中直接使用 getPromptContent 调用。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SectionPlanningService 导出 planSections 函数；接受 AppContext + projectId 参数；返回 PageSection[]；LLM 调用通过 resolveRouteProviderWithFallback + requestLlmPlainText；JSON 解析有严格校验；错误直接抛出而非降级。</done>
</task>

<task type="auto">
  <name>Task 3: 图片生成服务 + Step 4 路由 handlers</name>
  <files>src/modules/section-generation-service.ts, src/routes/image-project/step4-handlers.ts, src/routes/image-project/index.ts</files>
  <action>
1. 创建 `src/modules/section-generation-service.ts`，实现 Section 图片生成和版本管理：

**核心方法：**
```typescript
// 生成单个 section 的图片
async generateSectionImage(ctx, projectId, sectionId): Promise<{ section: PageSection }>

// 重新生成（清除旧版本，创建新版本）
async regenerateSectionImage(ctx, projectId, sectionId): Promise<{ section: PageSection }>

// 一键生成全部
async generateAllSections(ctx, projectId): Promise<{ sections: PageSection[] }>

// 持久化版本记录
async persistSectionVersion(ctx, section: PageSection, imageUrl: string): Promise<void>

// 列出某个 section 的版本历史
async listSectionVersions(ctx, sectionId: string): Promise<{ versions: SectionVersion[] }>

// 激活指定版本
async activateSectionVersion(ctx, sectionId: string, versionId: string): Promise<{ section: PageSection }>
```

generateSectionImage 实现逻辑：
- 获取 section，校验状态和 projectId
- 获取 step3 的 selectedPhotoIds 作为参考图片（通过 ctx.repos.modelPhotos.findByProjectId → 过滤 isSelected=true）
- 构建图片生成 prompt（使用 section.visualPrompt + section.copy 作为文本内容）
- 通过 resolveRouteProviderWithFallback(ctx, ['image_generation', 'text_to_image']) 获取 Provider
- 调用 requestJimengImageUrl 生成图片
- 通过 persistImageSourceToStorage 持久化
- 更新 section.currentImageUrl, section.status='success', section.currentVersionNumber++
- 调用 persistSectionVersion 创建版本记录
- **失败时直接 throw AppError，不做 SVG 降级**

persistSectionVersion 实现逻辑：
- 获取 section 当前 versionNumber + 1 作为新版本号
- 先将该 section 的旧版本 is_active 全部设为 false
- 创建新版本记录，is_active=true

2. 创建 `src/routes/image-project/step4-handlers.ts`，注册以下路由：

```typescript
export function registerImageProjectStep4Routes(app: FastifyInstance, ctx: AppContext): void {
  // 统一路由前缀: /image-projects/:projectId/step4/...

  // POST /step4/sections/plan — AI 规划页面结构
  // 1. requireUser + requireOwnerProject
  // 2. 获取搭配方案、模特图等上下文
  // 3. 调用 SectionPlanningService.planSections
  // 4. 持久化：删除旧 section → 批量创建新 section
  // 5. 更新 imageSteps.step4 快照
  // 6. 返回 sections 列表

  // GET /step4/sections — 查询所有 section（按 sortOrder）
  // 1. requireUser + requireOwnerProject
  // 2. ctx.repos.pageSections.findByProjectId
  // 3. 返回 { sections }

  // POST /step4/sections — 手动创建 section
  // 1. requireUser + requireOwnerProject
  // 2. 校验 body 中的 sectionType 必须是 11 种之一
  // 3. 创建 section，sortOrder 自动设为当前最大值 + 1
  // 4. 返回 { section }

  // PATCH /step4/sections/:sectionId — 更新 section
  // 1. requireUser + requireOwnerProject
  // 2. 校验 sectionId 属于当前项目
  // 3. 允许更新：title, copy, editableData（不允许更新 status, imageUrl）
  // 4. 返回 { section }

  // DELETE /step4/sections/:sectionId — 删除 section
  // 1. requireUser + requireOwnerProject
  // 2. 校验 sectionId 属于当前项目
  // 3. 删除 section 及其版本记录
  // 4. 返回 { success: true }

  // POST /step4/sections/:sectionId/generate — 生成图片
  // 1. requireUser + requireOwnerProject
  // 2. 校验 section 存在且属于当前项目
  // 3. 更新 status='generating'
  // 4. 调用 generateSectionImage
  // 5. 返回 { section }
  // 6. 失败时 status='failed', errorMessage=错误信息

  // POST /step4/sections/:sectionId/regenerate — 重新生成
  // 1. 同 generate，但强制创建新版本
  // 2. 保留旧版本记录

  // POST /step4/sections/generate-all — 一键生成全部
  // 1. requireUser + requireOwnerProject
  // 2. 获取所有 status !== 'success' 的 section
  // 3. 逐个生成，失败不阻塞其他 section
  // 4. 返回 { sections }

  // POST /step4/sections/reorder — 重新排序
  // 1. requireUser + requireOwnerProject
  // 2. body: { sectionIds: string[] }
  // 3. 按数组顺序更新每个 section 的 sortOrder
  // 4. 返回 { sections }

  // GET /step4/sections/:sectionId/versions — 版本列表
  // 1. requireUser + requireOwnerProject
  // 2. ctx.repos.sectionVersions.findBySectionId
  // 3. 返回 { versions }

  // PATCH /step4/sections/:sectionId/versions/:versionId/activate — 激活版本
  // 1. requireUser + requireOwnerProject
  // 2. 校验版本属于当前项目
  // 3. 激活版本：更新 section.currentImageUrl = version.imageUrl
  // 4. 返回 { section }
}
```

3. 修改 `src/routes/image-project/index.ts`：
   - import registerImageProjectStep4Routes from './step4-handlers.js'
   - 在 registerImageProjectRoutes 函数中调用 registerImageProjectStep4Routes(app, ctx)

**注意：** 所有 handler 必须：
- 入口调用 requireUser() + requireOwnerProject()
- 失败时 throw AppError，不静默降级
- 图片生成失败直接报错
- 使用 ctx.repos.pageSections 和 ctx.repos.sectionVersions（Task 1 注册）
</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SectionGenerationService 导出 6 个方法；step4-handlers.ts 注册 10 个路由；index.ts 引入 step4 路由注册；所有路由有权限校验；图片生成失败直接报错无降级；版本管理自动创建。</done>
</task>

</tasks>

<verification>
## 整体验证

1. TypeScript 编译通过：npx tsc --noEmit
2. 数据库表可用（nrm_page_sections, nrm_section_versions）
3. PgRepositoryCollection 包含 pageSections 和 sectionVersions
4. 路由注册：registerImageProjectStep4Routes 被 registerImageProjectRoutes 调用
5. 规划服务：planSections 返回有效的 PageSection[] 数组
6. 生成服务：generateSectionImage 失败时 throw AppError
7. 版本管理：每次生成自动创建 SectionVersion 记录
</verification>

<success_criteria>
- [ ] PageSection 和 SectionVersion 类型定义完成，11 种 SectionType 全部定义
- [ ] IPageSectionRepository 和 ISectionVersionRepository 接口定义完整
- [ ] PgPageSectionRepository 和 PgSectionVersionRepository 实现完整（CRUD + 批量操作）
- [ ] Repository 注册到 PgRepositoryCollection 和事务支持
- [ ] SectionPlanningService 通过 LLM 规划页面结构，JSON 校验严格
- [ ] SectionGenerationService 实现生成/重新生成/生成全部/版本管理
- [ ] 10 个路由 handlers 全部注册，有权限校验
- [ ] 图片生成失败直接报错，无 SVG 降级
- [ ] 提示词通过 getPromptContent 获取，不硬编码
- [ ] 所有文件 TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-step4-ecommerce-page/04-01-SUMMARY.md`
</output>
