---
phase: 04-step4-ecommerce-page
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/web/services/realApi/image-step4.ts
  - apps/web/services/realApi/index.ts
  - apps/web/pages/image-project/ImageEcommerceEditor.tsx
  - apps/web/pages/image-project/components/PhonePreview.tsx
  - apps/web/pages/image-project/components/SectionTree.tsx
  - apps/web/pages/image-project/components/SectionEditor.tsx
  - apps/web/pages/image-project/components/VersionHistory.tsx
autonomous: true
requirements:
  - IMG-04
  - IMG-06
must_haves:
  truths:
    - "进入 Step 4 页面后，自动触发 AI 规划（若无 section 数据）"
    - "左侧 320px 显示模块结构树，包含所有 section 及其状态"
    - "中间 flex 区域显示手机预览，真实手机外框 mockup"
    - "右侧 380px 显示编辑面板，可编辑选中 section 的内容"
    - "点击模块树中的 section，中间预览和右侧编辑面板联动更新"
    - "编辑面板可修改 title、copy、visualPrompt，保存后更新"
    - "编辑面板可点击生成/重新生成按钮，显示 loading 状态"
    - "编辑面板可查看版本历史，点击激活版本切换图片"
    - "支持一键生成全部 section"
  artifacts:
    - path: "apps/web/services/realApi/image-step4.ts"
      provides: "imageStep4Api 模块（10 个方法）"
      exports: ["imageStep4Api", "ImageStep4Api"]
    - path: "apps/web/pages/image-project/ImageEcommerceEditor.tsx"
      provides: "三栏编辑器主组件"
      contains: "xl:grid-cols-[320px_minmax(0,1fr)_380px]"
    - path: "apps/web/pages/image-project/components/PhonePreview.tsx"
      provides: "手机预览组件"
      contains: "export const PhonePreview"
    - path: "apps/web/pages/image-project/components/SectionTree.tsx"
      provides: "左侧模块树组件"
      contains: "export const SectionTree"
    - path: "apps/web/pages/image-project/components/SectionEditor.tsx"
      provides: "右侧编辑面板组件"
      contains: "export const SectionEditor"
    - path: "apps/web/pages/image-project/components/VersionHistory.tsx"
      provides: "版本历史面板组件"
      contains: "export const VersionHistory"
  key_links:
    - from: "ImageEcommerceEditor.tsx"
      to: "realBackendApi.imageStep4PlanSections"
      via: "自动规划触发"
      pattern: "imageStep4.*\\(token.*projectId"
    - from: "SectionTree.tsx"
      to: "ImageEcommerceEditor.tsx"
      via: "onSelect 回调传递 sectionId"
      pattern: "onSelect.*sectionId"
    - from: "SectionEditor.tsx"
      to: "realBackendApi"
      via: "生成/保存/版本切换 API 调用"
      pattern: "imageStep4.*generate|imageStep4.*save|imageStep4.*activate"
threat_model:
  Trust Boundaries:
    - Boundary: "client→API (section edit)"
      Description: "用户编辑的 title/copy 等字段传入后端，需校验长度和类型"
    - Boundary: "client→API (version activate)"
      Description: "versionId 需校验属于当前项目，不可激活其他项目的版本"

  STRIDE Threat Register:
    | Threat ID | Category | Component | Disposition | Mitigation Plan |
    | T-04-06 | Tampering | SectionEditor (edit inputs) | mitigate | 前端限制 title 长度 ≤ 100 字符，copy ≤ 2000 字符；后端也会校验 |
    | T-04-07 | Authorization | VersionHistory (activate) | mitigate | 后端验证 versionId 属于当前用户的项目；前端仅在用户点击时触发 |
    | T-04-08 | Denial of Service | ImageEcommerceEditor (generate-all) | mitigate | 逐张生成，显示进度指示；用户可随时中断页面 |
---

<objective>
实现 Step 4 电商详情页的前端三栏编辑器：模块树 + 手机预览 + 编辑面板，对接后端 API 完成完整交互闭环。

Purpose: 为用户提供可视化的电商详情页编辑体验，可独立控制每个 section 的生成、编辑和版本管理。
Output: 1 个 API 模块 + 5 个 React 组件文件，3 栏布局，每个组件 <300 行。
</objective>

<execution_context>
@.planning/get-shit-done/workflows/execute-plan.md
@.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-step4-ecommerce-page/04-CONTEXT.md

# API 参考
@apps/web/services/realApi/image-step3.ts — 图片项目 Step 3 API 模式
@apps/web/services/realApi/index.ts — realBackendApi 合并模式
@apps/web/services/backendApi.request.ts — request 函数

# 组件参考
@apps/web/pages/image-project/ImageModelPhotos.tsx — Step 3 页面组件模式
@apps/web/pages/image-project/ImageProjectLayout.tsx — 布局组件（自动保存逻辑）

# banana-mall 参考
@/tmp/banana-mall/components/editor/editor-workspace.tsx — 三栏编辑器参考
@/tmp/banana-mall/types/domain.ts — sectionTypes 和 sectionTypeLabels

# 类型定义（来自 Plan 04-01）
From src/contracts/types.ts:
```typescript
export type SectionType =
  | 'hero' | 'selling_points' | 'scenario' | 'detail_closeup'
  | 'specs' | 'material' | 'comparison' | 'gift_scene'
  | 'brand_trust' | 'summary' | 'custom';

export type SectionStatus = 'idle' | 'generating' | 'success' | 'failed';

export interface PageSection {
  id: string;
  projectId: string;
  sectionKey: string;
  sectionType: SectionType;
  title: string;
  goal: string;
  copy: string;
  visualPrompt: string;
  sortOrder: number;
  status: SectionStatus;
  currentImageUrl: string | null;
  currentVersionNumber: number;
  editableData: Record<string, unknown> | null;
  errorMessage: string | null;
  createdAt: number;
  updatedAt: number;
}

export interface SectionVersion {
  id: string;
  sectionId: string;
  projectId: string;
  versionNumber: number;
  imageUrl: string | null;
  promptSnapshot: Record<string, unknown> | null;
  copySnapshot: Record<string, unknown> | null;
  isActive: boolean;
  createdAt: number;
}
```

# 路由定义（来自 Plan 04-01）
- POST /image-projects/:projectId/step4/sections/plan — AI 规划
- GET /image-projects/:projectId/step4/sections — 查询所有
- POST /image-projects/:projectId/step4/sections — 创建
- PATCH /image-projects/:projectId/step4/sections/:sectionId — 更新
- DELETE /image-projects/:projectId/step4/sections/:sectionId — 删除
- POST /image-projects/:projectId/step4/sections/:sectionId/generate — 生成
- POST /image-projects/:projectId/step4/sections/:sectionId/regenerate — 重新生成
- POST /image-projects/:projectId/step4/sections/generate-all — 生成全部
- POST /image-projects/:projectId/step4/sections/reorder — 排序
- GET /image-projects/:projectId/step4/sections/:sectionId/versions — 版本列表
- PATCH /image-projects/:projectId/step4/sections/:sectionId/versions/:versionId/activate — 激活版本
</context>

<interfaces>
<!-- Key interfaces the executor needs for the frontend implementation. -->

From apps/web/services/realApi/image-step3.ts (API module pattern):
```typescript
export const imageStep3Api: ImageStep3Api = {
  imageStep3GenerateBatch(token, projectId, payload?) {
    return request<...>("POST", `/image-projects/${projectId}/step3/photos/generate-batch`, { token, body: payload ?? {} });
  },
  // ...
};
```

From apps/web/services/backendApi.request.ts:
```typescript
export function request<T>(method: "GET" | "POST" | "PATCH" | "DELETE", url: string, options?: { token?: string; body?: unknown }): Promise<T>;
```

From apps/web/store/useAppStore.ts:
```typescript
// Zustand store:
const token = useAppStore((state) => state.token);
const workflow = useAppStore((state) => state.workflow);
const projectData = useAppStore((state) => state.projectData);
const updateProjectData = useAppStore((state) => state.updateProjectData);
```

From apps/web/pages/image-project/ImageModelPhotos.tsx (status badge pattern):
```typescript
const statusLabelMap: Record<ModelPhotoStatus, string> = {
  pending: "等待中",
  generating: "生成中...",
  success: "已完成",
  failed: "生成失败",
};
```

From banana-mall editor-workspace.tsx (3-column layout):
```tsx
// 3-column grid layout
<div className="grid min-h-0 gap-6 xl:grid-cols-[320px_minmax(0,1fr)_380px] xl:items-stretch">
  {/* Left: 320px section tree */}
  {/* Middle: flex phone preview */}
  {/* Right: 380px editor panel */}
</div>
```

Section type labels (copy into the component):
```typescript
const SECTION_TYPE_LABELS: Record<SectionType, string> = {
  hero: "头图主视觉",
  selling_points: "卖点模块",
  scenario: "场景展示",
  detail_closeup: "细节特写",
  specs: "规格参数",
  material: "材质工艺",
  comparison: "对比说明",
  gift_scene: "送礼场景",
  brand_trust: "品牌信任",
  summary: "总结收口",
  custom: "自定义模块",
};
```

Section status labels:
```typescript
const SECTION_STATUS_LABELS: Record<SectionStatus, string> = {
  idle: "待生成",
  generating: "生成中...",
  success: "已完成",
  failed: "生成失败",
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Step 4 API 模块 + 注册到 realBackendApi</name>
  <files>apps/web/services/realApi/image-step4.ts, apps/web/services/realApi/index.ts</files>
  <action>
1. 创建 `apps/web/services/realApi/image-step4.ts`，定义 ImageStep4Api 接口和实现：

```typescript
import { request } from "../backendApi.request";
import type { PageSection, SectionVersion } from "../../../../src/contracts/types";

export interface ImageStep4Api {
  // AI 规划
  imageStep4PlanSections(token: string, projectId: string): Promise<{ sections: PageSection[] }>;
  // Section CRUD
  imageStep4ListSections(token: string, projectId: string): Promise<{ sections: PageSection[] }>;
  imageStep4CreateSection(token: string, projectId: string, body: { sectionType: string; title?: string }): Promise<{ section: PageSection }>;
  imageStep4UpdateSection(token: string, projectId: string, sectionId: string, body: { title?: string; copy?: string; editableData?: Record<string, unknown> }): Promise<{ section: PageSection }>;
  imageStep4DeleteSection(token: string, projectId: string, sectionId: string): Promise<{ success: boolean }>;
  // Image generation
  imageStep4GenerateSection(token: string, projectId: string, sectionId: string): Promise<{ section: PageSection }>;
  imageStep4RegenerateSection(token: string, projectId: string, sectionId: string): Promise<{ section: PageSection }>;
  imageStep4GenerateAll(token: string, projectId: string): Promise<{ sections: PageSection[] }>;
  imageStep4ReorderSections(token: string, projectId: string, sectionIds: string[]): Promise<{ sections: PageSection[] }>;
  // Version management
  imageStep4ListVersions(token: string, projectId: string, sectionId: string): Promise<{ versions: SectionVersion[] }>;
  imageStep4ActivateVersion(token: string, projectId: string, sectionId: string, versionId: string): Promise<{ section: PageSection }>;
}

export const imageStep4Api: ImageStep4Api = {
  // 实现每个方法，调用对应的 /image-projects/${projectId}/step4/... 路由
  // 参考 image-step3.ts 的模式，使用 request() 函数
};
```

所有方法的路径前缀：`/image-projects/${projectId}/step4/`

2. 在 `apps/web/services/realApi/index.ts` 中：
   - import { imageStep4Api, type ImageStep4Api } from './image-step4'
   - 在 RealBackendApi 类型交叉中添加 `& ImageStep4Api`
   - 在 realBackendApi 对象展开中添加 `...imageStep4Api`
  </action>
  <verify>
    <automated>cd /Users/Abner/HCode/neirongmiao/apps/web && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>imageStep4Api 导出 11 个方法；realBackendApi 类型包含 ImageStep4Api；TypeScript 编译通过无错误。</done>
</task>

<task type="auto">
  <name>Task 2: 三栏编辑器主组件 + 手机预览</name>
  <files>apps/web/pages/image-project/ImageEcommerceEditor.tsx, apps/web/pages/image-project/components/PhonePreview.tsx</files>
  <action>
1. 创建 `apps/web/pages/image-project/components/PhonePreview.tsx`（≤300 行）：

手机预览组件，模拟真实手机外框展示电商详情页。

Props 接口：
```typescript
interface PhonePreviewProps {
  sections: PageSection[];
  selectedSectionId: string | null;
}
```

实现要点：
- 外层使用手机 mockup 容器：圆角边框 + 阴影 + 顶部状态栏 + 底部导航栏模拟
- 内部垂直滚动，按 sortOrder 展示所有 section
- Hero 类型 section：显示 full-width 图片（aspect-[9/16]），无图片时显示灰色占位 + 标题
- 非 Hero 类型 section：显示 section 的 currentImageUrl 图片，无图片时显示虚线边框占位 + type label
- 选中的 section 在预览中高亮显示（紫色边框或光晕）
- 使用 Tailwind CSS 实现，不要引入新依赖
- 状态标签：生成中的 section 显示 loading spinner，失败的显示错误图标
- 底部模拟电商导航栏：客服 | 购物车 | 加入购物车（纯装饰性 UI）

2. 创建 `apps/web/pages/image-project/ImageEcommerceEditor.tsx`（≤300 行，复杂逻辑拆到子组件）：

三栏编辑器主组件，负责：
- 页面进入时检测：若 projectData.step4.sections 为空，自动调用 imageStep4PlanSections 触发 AI 规划
- 三栏布局：`grid min-h-0 gap-4 xl:grid-cols-[320px_minmax(0,1fr)_380px] xl:items-stretch`
- 状态管理：
  - sections: PageSection[]（从后端加载）
  - selectedSectionId: string | null（当前选中的 section）
  - isPlanning: boolean（AI 规划中）
  - isGeneratingAll: boolean（生成全部中）
- 生命周期：
  - useEffect：组件挂载时加载 sections（imageStep4ListSections）
  - useEffect：若 sections 为空且未规划过，自动触发 AI 规划
- 关键回调：
  - handleSelectSection(sectionId) → 设置 selectedSectionId
  - handleGenerateSection(sectionId) → 调用 imageStep4GenerateSection，更新本地状态
  - handleGenerateAll() → 调用 imageStep4GenerateAll，设置 loading 状态
  - handleUpdateSection(sectionId, fields) → 调用 imageStep4UpdateSection
  - handleDeleteSection(sectionId) → 调用 imageStep4DeleteSection
  - handleActivateVersion(sectionId, versionId) → 调用 imageStep4ActivateVersion
- 顶部工具栏：显示步骤指示器 + "生成全部" 按钮 + 返回按钮
- 左侧 <SectionTree>，中间 <PhonePreview>，右侧 <SectionEditor>

**重要：** ImageEcommerceEditor 本身不超过 300 行。状态管理和 API 调用逻辑保持简洁，具体 UI 交给 SectionTree、PhonePreview、SectionEditor 子组件。
  </action>
  <verify>
    <automated>cd /Users/Abner/HCode/neirongmiao/apps/web && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>PhonePreview 组件接收 sections 和 selectedSectionId，渲染手机外框 + section 预览；ImageEcommerceEditor 渲染三栏布局，自动加载 section 数据，AI 规划触发逻辑正确；两个文件都不超过 300 行；TypeScript 编译通过。</done>
</task>

<task type="auto">
  <name>Task 3: 模块树 + 编辑面板 + 版本历史</name>
  <files>apps/web/pages/image-project/components/SectionTree.tsx, apps/web/pages/image-project/components/SectionEditor.tsx, apps/web/pages/image-project/components/VersionHistory.tsx</files>
  <action>
1. 创建 `apps/web/pages/image-project/components/SectionTree.tsx`（≤300 行）：

Props 接口：
```typescript
interface SectionTreeProps {
  sections: PageSection[];
  selectedSectionId: string | null;
  onSelect: (sectionId: string) => void;
  onDelete: (sectionId: string) => void;
  onGenerate: (sectionId: string) => void;
  generatingSectionIds: Set<string>;
}
```

实现要点：
- 固定宽度 320px，垂直滚动列表
- 每个 section 项：
  - 左侧：type 图标 + label（使用 SECTION_TYPE_LABELS）
  - 中间：title（截断显示）+ status badge（idle/generating/success/failed）
  - 右侧：生成按钮（仅 idle/failed 状态显示）+ 删除按钮
- 选中项高亮背景
- 顶部有 "生成全部" 按钮（仅当存在未成功的 section 时显示）
- status badge 颜色：idle=gray, generating=blue(带spinner), success=green, failed=red
- 支持拖拽排序是 Phase 5 的事情，本阶段不做

2. 创建 `apps/web/pages/image-project/components/SectionEditor.tsx`（≤300 行）：

Props 接口：
```typescript
interface SectionEditorProps {
  section: PageSection | null;
  onUpdate: (sectionId: string, fields: { title?: string; copy?: string; editableData?: Record<string, unknown> }) => void;
  onGenerate: (sectionId: string) => void;
  onRegenerate: (sectionId: string) => void;
  onActivateVersion: (sectionId: string, versionId: string) => void;
  isGenerating: boolean;
}
```

实现要点：
- 固定宽度 380px，垂直滚动
- 未选中任何 section 时：显示 "请在左侧选择一个模块进行编辑"
- 选中 section 后显示：
  - 头部：section type label + status badge + 当前版本号
  - 类型选择器：11 种 section type 的下拉选择（可切换类型）
  - 标题输入框：Input 组件，绑定 section.title
  - 文案文本域：Textarea 组件，绑定 section.copy，maxLength=2000
  - 视觉提示词文本域：显示 section.visualPrompt，只读（由 AI 生成，用户不可直接编辑）
  - 操作按钮区：
    - "生成图片" 按钮（status=idle 时显示）
    - "重新生成" 按钮（status=success 时显示）
    - 两个按钮在生成中都显示 loading 状态并禁用
  - 当前图片预览：显示 section.currentImageUrl 的小图
  - 底部：<VersionHistory> 组件（内联或作为子组件引用）

3. 创建 `apps/web/pages/image-project/components/VersionHistory.tsx`（≤200 行）：

Props 接口：
```typescript
interface VersionHistoryProps {
  sectionId: string;
  projectId: string;
  activeVersionNumber: number;
  onActivate: (versionId: string) => void;
}
```

实现要点：
- 显示 "版本历史" 标题
- 点击展开/收起（默认收起）
- 展开后列出该 section 的所有版本（从后端加载）
- 每个版本项：版本号 + 创建时间 + 激活状态
  - 当前激活版本高亮显示
  - 非激活版本可点击切换
  - 点击调用 onActivate(versionId)
- 加载状态：显示 loading
- 空状态："暂无版本历史，生成图片后将自动创建版本"

**注意：** 所有组件使用 Tailwind CSS，不引入新 UI 库。使用 apps/web/components/ui/Button.tsx 作为按钮组件。toast 反馈使用 sonner（已在项目中使用）。所有操作必须有 loading 状态和成功/失败反馈。
  </action>
  <verify>
    <automated>cd /Users/Abner/HCode/neirongmiao/apps/web && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>SectionTree 展示所有 section 及状态，支持选中/生成/删除；SectionEditor 可编辑 title/copy，触发生成/重新生成，显示当前图片预览；VersionHistory 列出所有版本，支持激活切换；三个文件都不超过 300 行；TypeScript 编译通过。</done>
</task>

</tasks>

<verification>
## 整体验证

1. 前端 TypeScript 编译通过：cd apps/web && npx tsc --noEmit
2. imageStep4Api 11 个方法全部实现并注册到 realBackendApi
3. ImageEcommerceEditor 挂载后自动加载 sections 数据
4. 若无 section 数据，自动触发 AI 规划
5. 三栏布局正确渲染：320px + flex + 380px
6. 点击模块树中的 section，手机预览和编辑面板联动
7. 编辑面板可修改 title/copy 并保存到后端
8. 生成/重新生成按钮有 loading 状态和 toast 反馈
9. 版本历史可展开，点击版本号可切换激活版本
10. 所有组件文件 <300 行
</verification>

<success_criteria>
- [ ] imageStep4Api 模块 11 个方法实现完整，路径与后端路由一致
- [ ] realBackendApi 类型交叉包含 ImageStep4Api
- [ ] ImageEcommerceEditor 三栏布局正确渲染，320px + flex + 380px
- [ ] 进入 Step 4 自动加载 sections，为空时自动触发 AI 规划
- [ ] 模块树显示所有 section，状态正确（idle/generating/success/failed）
- [ ] 手机预览按 sortOrder 展示所有 section，选中项高亮
- [ ] 编辑面板可编辑 title（≤100 字符）和 copy（≤2000 字符）
- [ ] 生成/重新生成按钮有 loading 状态，成功/失败有 toast 反馈
- [ ] 一键生成全部按钮正常工作
- [ ] 版本历史可展开，点击可切换激活版本
- [ ] 所有组件文件 ≤300 行
- [ ] 前端 TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-step4-ecommerce-page/04-02-SUMMARY.md`
</output>
