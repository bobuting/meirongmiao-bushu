---
phase: 02-step1-step2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/image-project/index.ts
  - src/routes/image-project/step1-handlers.ts
  - src/routes/image-project/step2-handlers.ts
  - src/modules/project-service.ts
  - apps/web/pages/image-project/ImageAssets.tsx
  - apps/web/pages/image-project/ImageCharacterSelection.tsx
  - apps/web/pages/image-project/ImageProjectLayout.tsx
autonomous: true
requirements: [IMG-01, IMG-02]
---

<objective>
交付图片项目 Step 1 服装搭配和 Step 2 角色定妆的完整前后端流程（新写，不复用视频代码）

Purpose: 图片项目用户从上传商品图到完成角色定妆图，走独立于视频项目的简化流程
Output: 后端路由 handler + 精简前端组件 + 工作流自动保存
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-step1-step2/02-CONTEXT.md

# Phase 1 交付物（类型定义已就绪）
@src/contracts/project-step-snapshot.ts — ImageStep1Snapshot, ImageStep2Snapshot 已定义
@src/contracts/types.ts — ModelPhoto, CharacterPreview, CharacterViewKey 等已定义
@src/contracts/project-last-step.ts — IMAGE_PROJECT_MAX_STEP = 4

# 后端参考（复用服务，不复制代码）
@src/modules/outfit-service.ts — OutfitService.recommend()/select() 已实现
@src/modules/character-service.ts — CharacterService.generatePreviews()/confirmPreview() 已实现
@src/routes/step1-outfit/index.ts — 视频 Step 1 路由参考（结构复杂，只参考不复制）
@src/routes/step2-character/index.ts — 视频 Step 2 路由参考（结构复杂，只参考不复制）
@src/routes/project-flow-crud-routes.ts — buildProjectStepState(), saveWorkflowState() 参考
@src/routes/project-route-shared.ts — ProjectRouteDeps 接口（了解 deps 结构）
@src/modules/prompt/prompt-helper.ts — getPromptContent() 提示词管理
@src/modules/step1-image-clothing-guard.ts — 图片分类守卫
@src/modules/step1-role-direction-task.ts — 角色方向生成
@src/services/media/storage-persist.ts — persistImageSourceToStorage

# 前端参考
@src/contracts/step1-outfit-module-contract.ts — Step1OutfitModule 等类型
@src/contracts/step1-joint-reverse-contract.ts — Step1RoleDirectionCard 等
@src/contracts/step2-five-view-candidate-board-contract.ts — Step2FiveViewCandidateCard
@src/contracts/step2-prompt-parameter-variant-contract.ts — Step2 参数变体
@apps/web/pages/image-project/ImageAssets.tsx — 当前 3146 行，需精简
@apps/web/pages/image-project/ImageCharacterSelection.tsx — 当前 3118 行，需精简
@apps/web/pages/image-project/ImageProjectLayout.tsx — 布局组件，缺 auto-save
@apps/web/pages/image-project/imageProjectRouteNormalization.ts — 路由规范化
@apps/web/services/realApi/step1.ts — realStep1Api（classifyImage, removeBg, generateRoleDirection）
@apps/web/services/realApi/step2.ts — realStep2Api（recommendOutfits, selectOutfit, generatePreviews 等）
@apps/web/components/layout/LayoutWorkflowStepper.tsx — 工作流步进器
@apps/web/components/ui/Button.tsx — UI 按钮
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/contracts/project-step-snapshot.ts:
```typescript
/** 图片项目 Step 1: 服装搭配 */
export interface ImageStep1Snapshot {
  uploadedAssetIds: string[];
  outfitAnalysisResult: string | null;
  selectedOutfitPlanId: string | null;
  createdAt: number | null;
}

/** 图片项目 Step 2: 角色定妆 */
export interface ImageStep2Snapshot {
  characterPreviewIds: string[];
  selectedModelId: string | null;
  styledViewIds: string[];
  confirmedModel: boolean;
  createdAt: number | null;
}
```

From src/contracts/types.ts:
```typescript
export interface OutfitPlan {
  id: string;
  userId: string;
  projectId: string;
  assetIds: string[];
  index: number;
  title: string;
  reason: string;
  styleName: string;
  analysis: string;
  optimizedPrompt: string;
  suitableScene: string;
  tags: string[];
  items: OutfitPlanItem[];
  trendSummary?: string;
  groundingSources?: unknown[];
}

export interface CharacterPreview {
  id: string;
  projectId: string;
  userId: string;
  presetId: string;
  imageUrl: string;
  label?: string;
  sourceImageUrl?: string;
  viewKey?: CharacterViewKey; // "front" | "left" | "right" | "back" | "closeup"
}

export type CharacterViewKey = "front" | "left" | "right" | "back" | "closeup";
```

From src/modules/outfit-service.ts:
```typescript
class OutfitService {
  recommend(user, projectId, ctx, options?): Promise<OutfitPlan[]>;
  select(user, projectId, planId): Promise<OutfitPlan>;
}
```

From src/modules/character-service.ts:
```typescript
class CharacterService {
  listPresets(user): Promise<CharacterPreset[]>;
  generatePreviews(user, projectId, presetId): Promise<CharacterPreview[]>;
  confirmPreview(user, projectId, previewId): Promise<CharacterPreview>;
}
```

From apps/web/services/realApi/step2.ts (API contract):
```typescript
interface RealStep2Api {
  recommendOutfits(token, projectId, payload): Promise<{ plans: OutfitPlanDto[] }>;
  optimizeOutfitAnalysis(token, projectId, payload): Promise<{ analysis: string }>;
  selectOutfit(token, projectId, planId): Promise<{ id: string }>;
  unselectOutfit(token, projectId): Promise<{ ok: boolean }>;
  listPresets(token): Promise<{ presets: Array<{ id: string; name: string; tags: string[] }> }>;
  generatePreviews(token, projectId, presetId): Promise<{ previews, characterViews, styledViews, outfitSummary }>;
  confirmPreview(token, projectId, previewId): Promise<{ id: string; projectId: string }>;
  regenerateCharacterPreview(token, projectId, previewId, payload?): Promise<CharacterPreview>;
}
```

From apps/web/services/realApi/step1.ts (API contract):
```typescript
interface RealStep1Api {
  uploadAssets(token, projectId, files): Promise<{ assets }>;
  step1ClassifyImage(token, projectId, payload): Promise<Step1ImageClassificationResultDto>;
  step1RemoveBg(token, projectId, payload): Promise<Step1RemoveBgResultDto>;
  step1GenerateRoleDirection(token, projectId, selectedOutfitIndex, options?): Promise<{
    status: "completed"; roleDirectionCards: Step1RoleDirectionCard[]; adjustmentSuggestions: Step1RoleAdjustmentSuggestion[];
  }>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 图片项目路由骨架 — 注册 step1/step2 路由入口</name>
  <files>src/routes/image-project/index.ts</files>
  <action>
填充 `registerImageProjectRoutes()` 空壳，作为图片项目专用路由注册器。

**不要**导入 `registerStep1OutfitRoutes` 或 `registerStep2CharacterRoutes`（视频项目路由），图片项目需要独立的简化版 handler。

当前文件 `src/routes/image-project/index.ts` 已有 registrar 壳：
```typescript
export function registerImageProjectRoutes(app: FastifyInstance, ctx: AppContext): void {
  // TODO: Step 1, Step 2
}
export const imageProjectRouteRegistrar: RouteRegistrar = {
  id: "image_project_routes",
  register: registerImageProjectRoutes,
};
```

在此函数内添加导入和调用：
```typescript
import { registerImageProjectStep1Routes } from "./step1-handlers.js";
import { registerImageProjectStep2Routes } from "./step2-handlers.js";

registerImageProjectStep1Routes(app, ctx);
registerImageProjectStep2Routes(app, ctx);
```

注册器已存在于 `APP_ROUTE_REGISTRAR_IDS` 中（`"image_project_routes"`），无需修改 `src/routes/index.ts`。

验证：编译通过（`npx tsc --noEmit`），`registerImageProjectRoutes` 调用了两个子注册函数。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/routes/image-project/index.ts 导入了 step1-handlers 和 step2-handlers 并调用其注册函数，编译无错误</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Step 1 后端 — 服装上传、AI 搭配分析、图片分类、背景去除、角色方向生成</name>
  <files>src/routes/image-project/step1-handlers.ts</files>
  <behavior>
    - POST /image-projects/:projectId/step1/outfits/recommend → 调用 OutfitService.recommend()，返回 3 套搭配方案
    - POST /image-projects/:projectId/step1/outfits/select → 调用 OutfitService.select()，标记选中方案
    - POST /image-projects/:projectId/step1/outfits/unselect → 取消选中
    - POST /image-projects/:projectId/step1/classify-image → 调用图片分类（复用 step1-image-clothing-guard）
    - POST /image-projects/:projectId/step1/remove-bg → 调用背景去除（复用现有 remove-bg 逻辑）
    - POST /image-projects/:projectId/step1/role-direction → 调用角色方向生成（复用 step1-role-direction-task）
  </behavior>
  <action>
创建 `src/routes/image-project/step1-handlers.ts`，实现图片项目专用的 Step 1 路由 handler。

**设计原则**：比视频项目路由更简洁，直接调用 `ctx.outfitService` 和 `ctx.repos`，不经过 ProjectRouteDeps 中间层。

需要实现的 6 个路由（路径前缀 `/image-projects` 区别于视频项目的 `/projects`）：

**1. POST /image-projects/:projectId/step1/outfits/recommend**
```typescript
app.post("/image-projects/:projectId/step1/outfits/recommend", async (request) => {
  const user = await requireUser(ctx, request);
  const params = request.params as { projectId: string };
  // 检查是否已有搭配方案（决定是否 bypass cache）
  const existingPlans = await ctx.repos.outfitPlans.findByProjectId(params.projectId);
  const bypassCache = existingPlans.length > 0;
  // 调用 outfitService.recommend()
  const plans = await ctx.outfitService.recommend(user, params.projectId, ctx, { bypassCache });
  // 确保返回 3 套方案（不足时用 fallback 补齐，复用视频项目的补齐逻辑）
  // 转换 analysisCards 格式返回
  return { plans, analysisCards, taskStatus: "completed", analysisStatus: "ready" };
});
```
参考 `src/routes/step1-outfit/index.ts` L63-151 的 recommend handler，但简化掉视频特有的 garment asset 校验（图片项目用不同上传流程）。

**2. POST /image-projects/:projectId/step1/outfits/select**
```typescript
app.post("/image-projects/:projectId/step1/outfits/select", async (request) => {
  const user = await requireUser(ctx, request);
  const params = request.params as { projectId: string };
  const body = request.body as { planId: string };
  return await ctx.outfitService.select(user, params.projectId, body.planId);
});
```

**3. POST /image-projects/:projectId/step1/outfits/unselect**
参考视频项目 L160-169，直接操作 project 的 selectedOutfitPlanId 和 status。

**4. POST /image-projects/:projectId/step1/classify-image**
参考视频项目 L311-316，复用 `handleClassifyImage` 核心逻辑。图片项目只需要有 projectId 的版本。
导入并使用：
- `handleClassifyImage` 或直接复用 classify 逻辑
- `buildStep1ImageClassificationHeuristic`
- `buildStep1ImageClothingGuardResponse`

**5. POST /image-projects/:projectId/step1/remove-bg**
参考视频项目 L328-459，复用：
- `normalizeStep1RemoveBgRequest`（from step1-remove-bg-contract）
- `removeStep1ImageBackgroundToWhiteDataUrl`（from deps）
- `persistImageSourceToStorage`（from storage-persist）
- `recordRouteAudit`（from provider-resolver）

**6. POST /image-projects/:projectId/step1/role-direction**
参考视频项目 L469-546，复用：
- `generateStep1RoleDirectionCards`（from step1-role-direction-task）
- 获取已保存的 outfit plan，传入生成角色方向

每个路由都需要：
- `requireUser(ctx, request)` 鉴权
- `ctx.projectService.requireOwnerProject(user, projectId)` 项目归属校验
- 适当的错误处理（主流程失败直接报错，不降级）

**重要**：路由前缀用 `/image-projects` 而非 `/projects`，实现图片项目与视频项目的路由隔离。前端对应的 API 调用也需要指向 `/image-projects/...` 路径。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>step1-handlers.ts 包含 6 个路由 handler，均通过 auth 和 ownership 校验，编译无错误</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Step 2 后端 — 角色定妆图生成、五视图管理、确认</name>
  <files>src/routes/image-project/step2-handlers.ts</files>
  <behavior>
    - POST /image-projects/:projectId/step2/parameter-variants → 生成提示词参数变体
    - POST /image-projects/:projectId/step2/characters/previews → 生成角色预览五视图
    - POST /image-projects/:projectId/step2/characters/confirm → 确认选中的定妆图
    - POST /image-projects/:projectId/step2/characters/previews/:previewId/regenerate → 重新生成单张定妆图
  </behavior>
  <action>
创建 `src/routes/image-project/step2-handlers.ts`，实现图片项目专用的 Step 2 路由 handler。

**设计原则**：简化视频项目的 step2-character 路由，去除不需要的 library character 预设逻辑（图片项目直接基于 Step 1 的搭配方案生成定妆图）。

需要实现的 4 个路由：

**1. POST /image-projects/:projectId/step2/fixed-template-parameter-variants**
参考视频项目 step2-character/index.ts L64-177。
- 接收 `baseCoreFeatures`、`basePhase1Outfit`、`selectedRoleDirection`、`variantModes`
- 调用 `buildBaseStep2PromptParameterVariants()` 构建基础变体
- 调用 LLM 生成参数变体（通过 `getPromptContent("step2_prompt_variant", { userPrompt })`）
- 合并返回
- 失败时返回基础变体 + warning

**2. POST /image-projects/:projectId/step2/characters/previews**
参考视频项目 step2-character/index.ts L182-298。
- 验证项目已选中搭配方案（`project.selectedOutfitPlanId !== null`）
- 调用 `ctx.characterService.generatePreviews(user, projectId, presetId)`
- 获取搭配输入（`requireStep2StylingInputs`）
- 逐视角（五视图：front/left/right/back/closeup）生成定妆图
- 使用 `requestJimengImageUrl` 调用图片生成
- 持久化到存储（`persistDressedupViewImageToStorage`）
- 返回 previews、characterViews、styledViews、outfitSummary

**3. POST /image-projects/:projectId/step2/characters/confirm**
参考视频项目 step2-character/index.ts L303-308。
- 直接调用 `ctx.characterService.confirmPreview(user, projectId, previewId)`

**4. POST /image-projects/:projectId/step2/characters/previews/:previewId/regenerate**
参考视频项目 step2-character/index.ts L313-429。
- 验证 preview 归属
- 解析 viewKey（支持 front/left/right/back/closeup）
- 获取搭配输入
- 调用 `buildStep2StylingPromptAndImages` 构建生成参数
- 调用 `requestJimengImageUrl` 生成
- 持久化并返回

**关键导入**：
```typescript
import { resolveRouteProvider, resolveRouteProviderWithFallback, recordRouteAudit } from "../../services/llm/provider-resolver.js";
import { createLlmDebugRecord, finalizeLlmDebugRecordSuccess, finalizeLlmDebugRecordError } from "../../services/llm/llm-debug-recorder.js";
import { getPromptContent } from "../../modules/prompt/prompt-helper.js";
import { buildBaseStep2PromptParameterVariants, mergeStep2LlmPromptParameterVariants } from "../../contracts/step2-prompt-parameter-variant-contract.js";
import { extractJsonObject } from "../../services/utils/json-utils.js";
```

提示词必须通过 `getPromptContent()` 调用，禁止硬编码。PROMPT_CODE 使用 `"step2_prompt_variant"`（与视频项目一致）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>step2-handlers.ts 包含 4 个路由 handler，覆盖五视图生成和确认流程，编译无错误</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Step 1+2 前端精简 — 图片项目专用页面组件 + 工作流自动保存</name>
  <files>apps/web/pages/image-project/ImageAssets.tsx
apps/web/pages/image-project/ImageCharacterSelection.tsx
apps/web/pages/image-project/ImageProjectLayout.tsx
apps/web/services/realApi/step1.ts
apps/web/services/realApi/step2.ts</files>
  <action>
**4A. 修改前端 API 层指向图片项目路由**

修改 `apps/web/services/realApi/step1.ts` 和 `apps/web/services/realApi/step2.ts` 中的请求路径，从 `/projects/${projectId}/...` 改为 `/image-projects/${projectId}/...`，使前端 API 调用指向 Task 2/3 新建的图片项目专用路由。

具体修改：
- `step1.ts` 中：`uploadAssets`, `step1ClassifyImage`, `step1RemoveBg`, `step1GenerateRoleDirection` 的路径前缀改为 `/image-projects/${projectId}/step1/...`
- `step2.ts` 中：`recommendOutfits`, `optimizeOutfitAnalysis`, `selectOutfit`, `unselectOutfit`, `listPresets`, `generatePreviews`, `confirmPreview`, `regenerateCharacterPreview` 的路径前缀改为 `/image-projects/${projectId}/...`
  - 注意：`listPresets` 路径从 `/characters/presets` 保持不变（不需要 projectId 前缀）

**4B. 精简 ImageAssets.tsx（Step 1 页面）**

当前 3146 行，需精简到约 1000-1500 行。

精简策略（保留图片项目必要逻辑，删除视频项目专用逻辑）：
1. **删除视频项目特有模块**：
   - `step1OutfitModules` 多主体多方案复杂逻辑（图片项目保持简单，单主体单方案）
   - 复杂的 `Step1RoleDirectionDrawerPanel` 抽屉逻辑
   - `step1LegacyUploadPreviewBridge` 兼容逻辑
   - `step1ModuleSubjectBackfillPatch` 多主体回填
   - 信用积分/credits 计费相关（图片项目暂不需要）
   - Admin debug prompt 面板（Deferred）

2. **保留核心流程**：
   - 图片上传 → OSS 上传 → 调用 `uploadAssets`
   - 图片分类 → `step1ClassifyImage` → 显示分类结果
   - 背景去除 → `step1RemoveBg` → 显示处理后图片
   - 搭配推荐 → `recommendOutfits` → 展示 3 套方案卡片
   - 选择方案 → `selectOutfit` → 更新状态
   - 角色方向生成 → `step1GenerateRoleDirection` → 展示角色方向卡片
   - 下一步导航 → `buildImageProjectCanonicalRoute(2, projectId)` 进入 Step 2

3. **状态管理简化**：
   - 使用 `projectData` 中的 `uploads`, `generatedOutfits`, `outfitAnalysisCards`, `selectedOutfitId` 字段
   - 去除 `step1OutfitModules` 复杂模块管理
   - 保持 `useAppStore` 的 `updateWorkflow`/`updateProjectData` 调用

**4C. 精简 ImageCharacterSelection.tsx（Step 2 页面）**

当前 3118 行，需精简到约 1000-1500 行。

精简策略：
1. **删除视频项目特有模块**：
   - 复杂的五视图候选板 `Step2FiveViewCandidateBoard` 相关逻辑
   - `step2AllInOneBatchScheduler` 批量调度器
   - `step2SlotRuntimeIsolation` 插槽隔离
   - `step2GenerationScheduler` 生成调度
   - 信用积分/credits 计费相关
   - Admin debug prompts

2. **保留核心流程**：
   - 加载已选搭配方案信息
   - 调用 `generatePreviews` 生成角色定妆图（五视图）
   - 五视图展示（正面/侧面/背面/45度/全身）
   - 重新生成单张 → `regenerateCharacterPreview`
   - 确认定妆图 → `confirmPreview`
   - 完成项目导航

3. **状态管理简化**：
   - 使用 `projectData` 中的 `selectedModelId`, `confirmedModel`, `step2StyledViews`, `step2CharacterViews`
   - 去除复杂的生成依赖管理

**4D. ImageProjectLayout 添加自动保存**

当前 `ImageProjectLayout.tsx` 缺少工作流状态自动保存，导致页面刷新后状态丢失。参考 `ProjectLayout.tsx` 的 auto-save 逻辑添加：

```typescript
// 在 ImageProjectLayout 中添加：
const latestWorkflowPayloadRef = useRef<...>(null);

// 监听 workflow 和 projectData 变化，序列化后存储到 ref
useEffect(() => {
  const serialized = JSON.stringify({ workflow, projectData });
  latestWorkflowPayloadRef.current = { workflow, projectData, serialized };
}, [workflow, projectData]);

// 定时同步到后端
useEffect(() => {
  const interval = setInterval(() => {
    const latest = latestWorkflowPayloadRef.current;
    if (!latest || !latest.token || !latest.projectId) return;
    if (syncedWorkflowStateRef.current === latest.serialized) return;
    backendApi.saveProjectWorkflowState(latest.token, latest.projectId, latest.payload)
      .catch(() => {});
  }, 3000);
  return () => clearInterval(interval);
}, []);
```

**4E. 路由导航确保**

确保 Step 1 完成后能导航到 Step 2，Step 2 完成后能导航到 Step 3（预留）：
- Step 1 的"下一步"按钮 → `navigate(buildImageProjectCanonicalRoute(2, projectId))`
- Step 2 的"完成"按钮 → `navigate(buildImageProjectCanonicalRoute(3, projectId))` 或跳转到项目广场

**4F. 数据流向确认**

确认前端 `updateProjectData` 调用的字段能被后端 `buildProjectStepState`（kind=image 分支）正确读取为 step1/step2 state：
- Step 1 字段：`uploads`, `uploadLibraryAssetIds`, `generatedOutfits`, `outfitAnalysisCards`, `selectedOutfitId`, `outfitSummary`
- Step 2 字段：`selectedModelId`, `confirmedModel`, `step2StyledViews`, `step2CharacterViews`

这些字段已通过 Phase 1 的 `buildProjectStepState` kind=image 分支的 `pickProjectStepState(["step1"])` / `pickProjectStepState(["step2"])` 机制保存。
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ImageAssets.tsx 和 ImageCharacterSelection.tsx 行数减少 50%+，ImageProjectLayout 有 auto-save，前端编译无错误，导航可访问 /image-create/:projectId/step1 和 step2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → 后端 API | 用户上传的图片 URL、搭配选择、定妆确认均为不可信输入 |
| 后端 → LLM 服务 | prompt 内容可能包含用户注入的恶意内容 |
| 后端 → 对象存储 | 图片持久化路径需要防目录穿越 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | /image-projects/:projectId/step1/* | mitigate | 所有路由使用 `requireUser(ctx, request)` + `requireOwnerProject` 双重鉴权 |
| T-02-02 | Tampering | outfit select/unselect | mitigate | `requireOwnerProject` 校验 projectId 归属，防止越权选择他人项目 |
| T-02-03 | Information Disclosure | character previews | mitigate | preview 查询/生成均校验 `userId === user.id`，不暴露他人定妆图 |
| T-02-04 | Repudiation | LLM 调用审计 | mitigate | 使用 `recordRouteAudit` 和 `createLlmDebugRecord` 记录所有 LLM 调用（成功和失败） |
| T-02-05 | Injection | LLM prompt 构建 | mitigate | 提示词通过 `getPromptContent()` 从数据库模板获取，用户输入通过 `userPrompt` 变量注入，不拼接 system prompt |
| T-02-06 | Path Traversal | 图片持久化路径 | mitigate | `persistImageSourceToStorage` 使用 `projects/${projectId}/step1/` 固定前缀，不接受用户自定义路径 |
</threat_model>

<verification>
## Overall Phase Checks

1. **路由隔离验证**：
   - `curl -X POST http://localhost:3020/image-projects/test/step1/outfits/recommend` 返回 401（未登录）
   - 图片项目路由前缀为 `/image-projects`，视频项目为 `/projects`，路径不冲突

2. **Step 1 完整流程验证**：
   - 上传图片 → 分类 → 去背景 → 搭配推荐 → 选择方案 → 角色方向生成
   - 每个步骤的 API 调用返回正确状态码和数据

3. **Step 2 完整流程验证**：
   - 基于选中搭配 → 生成五视图预览 → 重新生成单张 → 确认定妆
   - 五视图包含 front/left/right/back/closeup 五个视角

4. **前端精简验证**：
   - ImageAssets.tsx 行数 < 1500 行
   - ImageCharacterSelection.tsx 行数 < 1500 行
   - 编译无 TypeScript 错误

5. **工作流保存验证**：
   - ImageProjectLayout 有 auto-save 逻辑（setInterval 3s）
   - 页面刷新后 step state 能正确恢复
</verification>

<success_criteria>
1. 用户可以上传图片并完成 AI 搭配分析，输出标准化搭配方案 — Task 2 + Task 4B
2. 用户可以基于搭配方案生成角色定妆图，支持五视图展示 — Task 3 + Task 4C
3. Step 1 确认后可进入 Step 2，Step 2 确认后可进入 Step 3 — Task 4F（导航）
4. 图片项目路由 `/image-create/:projectId/step1` 和 `/image-create/:projectId/step2` 可正常访问 — Task 4B/4C/4D
5. 数据存储到独立的 step1/step2 字段，不与视频项目混用 — 后端路由使用 `/image-projects` 前缀，前端使用 `pickProjectStepState(["step1"])` / `pickProjectStepState(["step2"])`
</success_criteria>

<output>
After completion, create `.planning/phases/02-step1-step2/02-01-SUMMARY.md`
</output>
