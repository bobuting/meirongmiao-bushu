---
phase: 03-mapper-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/video-reverse-core/mapper.ts, src/modules/video-reverse-core/index.ts]
autonomous: true
requirements: [MAPR-01, MAPR-02, MAPR-03]
user_setup: []
must_haves:
  truths:
    - "BatchSyncMapper 将 CoreReverseOutput 转换为 LlmReverseResult 格式"
    - "CloneMapper 将 CoreReverseOutput 转换为 SingleVideoReverseResult 格式"
    - "SquareRouteMapper 返回 NOT_IMPLEMENTED 占位结果"
    - "所有映射器为纯函数，无副作用"
    - "错误码直接映射，不做额外转换"
  artifacts:
    - path: "src/modules/video-reverse-core/mapper.ts"
      provides: "三个输出映射函数"
      exports: ["mapToBatchResult", "mapToCloneResult", "mapToSquareResult", "SQUARE_ROUTE_MAPPER_STATUS"]
      min_lines: 50
    - path: "src/modules/video-reverse-core/index.ts"
      provides: "公共导出更新"
      contains: "export { mapToBatchResult"
  key_links:
    - from: "src/modules/video-reverse-core/mapper.ts"
      to: "CoreReverseOutput"
      via: "import from types.js"
      pattern: "import.*CoreReverseOutput.*from.*types"
    - from: "src/modules/video-reverse-core/mapper.ts"
      to: "LlmReverseOutput"
      via: "import from normalize-output.js"
      pattern: "import.*LlmReverseOutput.*from.*normalize-output"
---

<objective>
创建输出映射层，将核心管道输出 CoreReverseOutput 转换为各入口点特定格式。

Purpose: 实现单一维护点的输出转换，三个入口点共享同一映射逻辑。
Output: mapper.ts 文件包含三个纯函数映射器。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mapper-layer/03-CONTEXT.md
@.planning/phases/03-mapper-layer/03-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/modules/video-reverse-core/types.ts:
```typescript
export interface CoreReverseOutput {
  rawLlmOutput: unknown | null;
  resolvedVideoUrl: string;
  success: boolean;
  errorCode: CoreReverseErrorCode | null;
  errorMessage: string | null;
}

export type CoreReverseErrorCode =
  | "VIDEO_DOWNLOAD_FAILED"
  | "NO_PROVIDER"
  | "LLM_RESPONSE_INVALID"
  | "LLM_CALL_FAILED";
```

From src/modules/video-reverse-core/normalize-output.ts:
```typescript
export type LlmReverseOutput = VideoHotTrendAnalysisOutputFull & {
  hot_trend_labels: {
    suitability: HotTrendSuitability;
    humanPresence: HotTrendHumanPresence;
    humanExposure: HotTrendHumanExposure;
    labels: string[];
    reason: string;
  };
};
```

From src/modules/video-hot-trend/sync-service.ts (lines 83-93):
```typescript
interface LlmReverseResult {
  videoKey: string;       // 去重键
  videoTitle: string;     // 视频标题
  rank: number;
  sourceUrl: string;      // 视频URL
  ossUrl: string | null;  // OSS 公开 URL
  status: "success" | "failed";
  output: LlmReverseOutput | null;
  errorCode: string | null;
  errorMessage: string | null;
}
```

From src/modules/video-hot-trend/single-reverse-service.ts (lines 129-190):
```typescript
export interface SingleVideoReverseResult {
  id: string;
  projectId: string | null;
  input: string;
  status: string;
  scriptVersionId: string | null;
  libraryScriptId: string | null;
  reverseStoryboardLibraryId: string | null;
  rawLlmOutput: LlmReverseOutput | null;
  storyboardPanel: {
    source: { sourceType: "video_url"; videoUrl: string; filename: string | null; mimeType: string | null; duration: number | null; };
    report: { intro: string | null; sections: SingleVideoReverseResultSection[]; frames: SingleVideoReverseResultFrame[]; rawMarkdown: string; hasStructuredSections: boolean; };
    diagnostics: unknown;
    raw: unknown;
  } | null;
  libraryScript: { id: string; title: string; content: string; tags: string[]; date: number; } | null;
  resolvedVideoUrl: string;
  fallback: boolean;
  code: string | undefined;
  message: string | undefined;
  inputMode: "video_url";
  scriptHints: { source: string; overviews: string[]; itemCount: number; primaryItem: { url: string; title: string; videoUrl: string; ... } } | null;
}
```

From src/modules/video-reverse-core/square-route-adapter.ts:
```typescript
export const SQUARE_ROUTE_ADAPTER_STATUS = "NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 mapper.ts 输入接口和类型定义</name>
  <files>src/modules/video-reverse-core/mapper.ts</files>
  <read_first>
    - src/modules/video-reverse-core/types.ts
    - src/modules/video-reverse-core/normalize-output.ts
    - src/modules/video-hot-trend/sync-service.ts
    - src/modules/video-hot-trend/single-reverse-service.ts
  </read_first>
  <action>
    创建 mapper.ts 文件，首先定义三个映射器的输入接口：

    1. **BatchSyncMapperInput** (per D-04 最小参数设计):
       ```typescript
       export interface BatchSyncMapperInput {
         coreOutput: CoreReverseOutput;
         videoKey: string;      // 入口特定：去重键
         videoTitle: string;    // 入口特定：视频标题
         rank: number;          // 入口特定：排名
         sourceUrl: string;     // 入口特定：原始 URL
         ossUrl: string | null; // 入口特定：OSS 上传结果
       }
       ```

    2. **CloneMapperInput** (per D-04):
       ```typescript
       export interface CloneMapperInput {
         coreOutput: CoreReverseOutput;
         videoUrl: string;      // 入口特定：解析后的视频 URL
         generateId: () => string; // 入口特定：ID 生成函数
         now: () => number;     // 入口特定：时间戳函数
       }
       ```

    3. 导入必要的类型（使用 .js 扩展名 per ESM 规范）：
       ```typescript
       import type { CoreReverseOutput } from "./types.js";
       import type { LlmReverseOutput } from "./normalize-output.js";
       import type { SingleVideoReverseResult, SingleVideoReverseResultSection, SingleVideoReverseResultFrame } from "../video-hot-trend/single-reverse-service.js";
       ```

    4. 添加文件头注释说明映射器设计原则（per D-03 纯函数设计）。
  </action>
  <acceptance_criteria>
    - `grep -c "interface BatchSyncMapperInput" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "interface CloneMapperInput" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "import.*CoreReverseOutput" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "import.*LlmReverseOutput" src/modules/video-reverse-core/mapper.ts` 返回 1
    - 文件开头包含中文注释说明设计原则
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/modules/video-reverse-core/mapper.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    mapper.ts 文件创建完成，包含两个输入接口定义和必要的类型导入。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 实现 mapToBatchResult 和 mapToCloneResult 函数</name>
  <files>src/modules/video-reverse-core/mapper.ts</files>
  <read_first>
    - src/modules/video-reverse-core/mapper.ts (当前进度)
    - src/modules/video-hot-trend/single-reverse-service.ts (lines 339-502, mapLlmReverseToResult 参考实现)
    - src/modules/video-hot-trend/sync-service.ts (lines 83-93, LlmReverseResult 类型)
    - src/modules/video-reverse-core/types.ts
  </read_first>
  <action>
    在 mapper.ts 中实现两个核心映射函数：

    **1. mapToBatchResult** (MAPR-01, per D-05, D-08):

    ```typescript
    /**
     * 将核心管道输出映射为批量入库格式
     * 纯函数，无副作用 (per D-03)
     * 错误码直接映射，不做额外转换 (per D-05, D-08)
     */
    export function mapToBatchResult(input: BatchSyncMapperInput): LlmReverseResult {
      if (!input.coreOutput.success) {
        // 错误情况：直接映射 errorCode 和 errorMessage
        return {
          videoKey: input.videoKey,
          videoTitle: input.videoTitle,
          rank: input.rank,
          sourceUrl: input.sourceUrl,
          ossUrl: input.ossUrl,
          status: "failed",
          output: null,
          errorCode: input.coreOutput.errorCode,
          errorMessage: input.coreOutput.errorMessage,
        };
      }

      // 成功情况：标准化输出类型转换
      const output = input.coreOutput.rawLlmOutput as LlmReverseOutput;
      return {
        videoKey: input.videoKey,
        videoTitle: input.videoTitle,
        rank: input.rank,
        sourceUrl: input.sourceUrl,
        ossUrl: input.ossUrl,
        status: "success",
        output: output,
        errorCode: null,
        errorMessage: null,
      };
    }
    ```

    在文件内定义 LlmReverseResult 接口（与 sync-service.ts 保持一致）。

    **2. mapToCloneResult** (MAPR-03, 复用 mapLlmReverseToResult 模式):

    参考 single-reverse-service.ts 的 mapLlmReverseToResult 函数实现，适配为最小参数设计：

    - 错误情况：构建包含 code/message 的 SingleVideoReverseResult
    - 成功情况：复用 frames、sections、libraryScript、scriptHints 构建逻辑
    - 使用 safe casting 模式处理嵌套结构：`(output.video_info ?? {}) as unknown as Record<string, unknown>`
    - 使用 input.generateId() 和 input.now() 替代 deps 方法

    实现要点（从 lines 339-502 提取核心逻辑）：
    - 从 LlmReverseOutput 提取 videoInfo, editingAnalysis, shotBreakdown
    - 构建 frames 数组（遍历 shot_breakdown）
    - 构建 5 个标准 sections（positioning, rhythm, hook, replica, optimization）
    - 构建 storyboardPanel 结构
    - 构建 libraryScript（从 hot_trend_labels.labels 提取 tags）
    - 构建 scriptHints 结构

    注意：SingleVideoReverseResult.rawLlmOutput 在错误情况为 null，成功情况为实际输出。
  </action>
  <acceptance_criteria>
    - `grep -c "export function mapToBatchResult" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "export function mapToCloneResult" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "status: \"failed\"" src/modules/video-reverse-core/mapper.ts` 返回至少 2（两个函数都有错误处理）
    - `grep -c "errorCode: input.coreOutput.errorCode" src/modules/video-reverse-core/mapper.ts` 返回 1（直接映射 per D-08）
    - `grep -c "shotBreakdown" src/modules/video-reverse-core/mapper.ts` 返回至少 1（CloneMapper 处理分镜）
    - `grep -c "hot_trend_labels" src/modules/video-reverse-core/mapper.ts` 返回至少 1（CloneMapper 处理标签）
    - TypeScript 编译通过，无 `any` 类型
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(error|Error)" | head -10 || echo "TypeScript compilation passed"</automated>
  </verify>
  <done>
    mapToBatchResult 和 mapToCloneResult 函数实现完成，TypeScript 类型检查通过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 实现 mapToSquareResult 占位函数并更新导出</name>
  <files>src/modules/video-reverse-core/mapper.ts, src/modules/video-reverse-core/index.ts</files>
  <read_first>
    - src/modules/video-reverse-core/mapper.ts (当前进度)
    - src/modules/video-reverse-core/square-route-adapter.ts (NOT_IMPLEMENTED 模式参考)
    - src/modules/video-reverse-core/index.ts (当前导出)
  </read_first>
  <action>
    **1. 实现 mapToSquareResult 占位函数** (MAPR-02, per D-06, D-07):

    在 mapper.ts 中添加占位函数，与 Phase 2 SquareRouteAdapter NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH 保持对称：

    ```typescript
    /**
     * 广场路由输出映射器 — 占位实现
     * STATUS: NOT_IMPLEMENTED
     * 原因：与 Phase 2 SquareRouteAdapter NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH 状态保持对称 (per D-06, D-07)
     * Phase 4 整合时处理
     */
    export function mapToSquareResult(_coreOutput: CoreReverseOutput): Record<string, unknown> {
      return {
        status: "error",
        code: "NOT_IMPLEMENTED",
        message: "SquareRouteMapper not implemented - Phase 4 integration required",
        resolvedVideoUrl: _coreOutput.resolvedVideoUrl,
        fallback: false,
      };
    }

    /**
     * 标记常量（用于类型检查和文档追踪）
     * 与 SQUARE_ROUTE_ADAPTER_STATUS 保持对称 (per D-07)
     */
    export const SQUARE_ROUTE_MAPPER_STATUS = "NOT_IMPLEMENTED";
    export const SQUARE_ROUTE_MAPPER_REASON =
      "SquareRouteAdapter is NOT_IMPLEMENTED_ARCHITECTURAL_MISMATCH (Phase 2); " +
      "mapper deferred to Phase 4 integration";
    ```

    **2. 更新 index.ts 导出**:

    在 src/modules/video-reverse-core/index.ts 中添加映射器导出：

    ```typescript
    // 映射器
    export {
      mapToBatchResult,
      mapToCloneResult,
      mapToSquareResult,
      SQUARE_ROUTE_MAPPER_STATUS,
      SQUARE_ROUTE_MAPPER_REASON,
    } from "./mapper.js";

    // 映射器输入接口
    export type { BatchSyncMapperInput, CloneMapperInput } from "./mapper.js";
    ```

    确保导出顺序合理，在适配器导出之后添加。
  </action>
  <acceptance_criteria>
    - `grep -c "export function mapToSquareResult" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "SQUARE_ROUTE_MAPPER_STATUS" src/modules/video-reverse-core/mapper.ts` 返回 1
    - `grep -c "NOT_IMPLEMENTED" src/modules/video-reverse-core/mapper.ts` 返回至少 2
    - `grep -c "mapToBatchResult" src/modules/video-reverse-core/index.ts` 返回 1
    - `grep -c "mapToCloneResult" src/modules/video-reverse-core/index.ts` 返回 1
    - `grep -c "mapToSquareResult" src/modules/video-reverse-core/index.ts` 返回 1
    - `grep -c "BatchSyncMapperInput" src/modules/video-reverse-core/index.ts` 返回 1
    - TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(error|Error)" | head -10 || echo "TypeScript compilation passed"</automated>
  </verify>
  <done>
    mapper.ts 包含完整三个映射函数，index.ts 导出更新完成，所有 TypeScript 类型检查通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoreReverseOutput → Mapper | 数据来自核心管道（Phase 1 已完成），可信输入 |
| Mapper → Entry-specific format | 内部结构转换，无外部暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | mapper.ts input validation | accept | 纯函数处理可信核心管道输出，无外部输入 |
| T-03-02 | Information Disclosure | error code mapping | accept | errorCode 仅用于内部错误追踪，无敏感信息 |
| T-03-03 | Denial of Service | mapper execution | accept | 纯函数无 I/O 操作，不可能阻塞 |

**Note:** 映射器层无安全风险 — 纯函数处理已验证的核心管道输出，不涉及外部 API、数据库或用户输入。
</threat_model>

<verification>
## Phase-level verification

1. **Type safety check:**
   ```bash
   npx tsc --noEmit
   ```

2. **Export completeness:**
   ```bash
   grep -E "mapTo|Mapper" src/modules/video-reverse-core/index.ts
   ```

3. **Function count verification:**
   ```bash
   grep -c "export function map" src/modules/video-reverse-core/mapper.ts
   ```
   Expected: 3 (mapToBatchResult, mapToCloneResult, mapToSquareResult)

4. **Pure function verification:**
   ```bash
   grep -E "(await|async|fetch|db|query)" src/modules/video-reverse-core/mapper.ts
   ```
   Expected: 0 (纯函数无异步或外部调用)
</verification>

<success_criteria>
- [ ] mapper.ts 文件存在于 src/modules/video-reverse-core/
- [ ] 三个导出函数：mapToBatchResult, mapToCloneResult, mapToSquareResult
- [ ] BatchSyncMapperInput 和 CloneMapperInput 接口定义
- [ ] SQUARE_ROUTE_MAPPER_STATUS 常量导出
- [ ] TypeScript 编译无错误
- [ ] index.ts 包含所有映射器导出
- [ ] 无 `any` 类型使用
- [ ] 所有函数为纯函数（无 async/await/外部调用）
</success_criteria>

<output>
After completion, create `.planning/phases/03-mapper-layer/03-01-SUMMARY.md`
</output>