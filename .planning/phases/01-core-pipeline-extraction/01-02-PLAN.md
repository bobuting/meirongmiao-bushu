---
phase: 01-core-pipeline-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/video-reverse-core/normalize-output.ts]
autonomous: true
requirements: [CORE-03]
user_setup: []

must_haves:
  truths:
    - "normalizeLlmReverseOutput 函数存在于核心模块"
    - "函数可被外部模块导入使用"
    - "LlmReverseOutput 类型定义完整"
  artifacts:
    - path: "src/modules/video-reverse-core/normalize-output.ts"
      provides: "LLM 输出标准化函数"
      exports: ["normalizeLlmReverseOutput", "LlmReverseOutput"]
      min_lines: 80
  key_links:
    - from: "normalize-output.ts"
      to: "video-hot-trend/types.ts"
      via: "import { VideoHotTrendAnalysisOutputFull } from '../../video-hot-trend/types.js'"
      pattern: "import.*types"
    - from: "normalize-output.ts"
      to: "contracts/hot-trend-base.ts"
      via: "import { HotTrendSuitability, HotTrendHumanPresence, HotTrendHumanExposure }"
      pattern: "import.*hot-trend-base"
---

<objective>
将 normalizeLlmReverseOutput 函数从 sync-service.ts 移动到核心模块，实现单点维护。

Purpose: 该函数已在两个入口点间共享（export/import），移动到核心模块实现统一维护点
Output: normalize-output.ts 包含 normalizeLlmReverseOutput 函数和 LlmReverseOutput 类型定义
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-pipeline-extraction/01-CONTEXT.md
@.planning/phases/01-core-pipeline-extraction/01-RESEARCH.md

<interfaces>
<!-- Source of truth: the function to extract -->

From src/modules/video-hot-trend/sync-service.ts (lines 103-166):
```typescript
export function normalizeLlmReverseOutput(raw: unknown): LlmReverseOutput {
  const record = (raw ?? {}) as Record<string, unknown>;
  // 透传原始 LLM 嵌套结构
  const videoAnalysis = record.video_analysis as unknown | undefined;
  const editingAnalysis = record.editing_analysis as unknown | undefined;
  const shotBreakdown = Array.isArray(record.shot_breakdown) ? record.shot_breakdown : [];
  const videoInfo = record.video_info as unknown | undefined;

  // ---- hot_trend_labels 推导 ----
  const va = (videoAnalysis ?? {}) as Record<string, unknown>;
  const onScreenPresence = (va.on_screen_presence ?? {}) as Record<string, unknown>;
  const fashionPlacement = (va.fashion_placement ?? {}) as Record<string, unknown>;

  const hasRealPerson = onScreenPresence.has_real_person;
  const exposureLevel = onScreenPresence.exposure_level;
  const isFashionSuitable = fashionPlacement.suitable;

  const suitability: HotTrendSuitability =
    isFashionSuitable === true ? "high"
      : isFashionSuitable === false ? "low"
      : "medium";

  const humanPresence: HotTrendHumanPresence =
    hasRealPerson === true ? "yes"
      : hasRealPerson === false ? "no"
      : "uncertain";

  const humanExposure: HotTrendHumanExposure =
    typeof exposureLevel === "string"
      ? exposureLevel.includes("高") ? "large"
        : exposureLevel.includes("低") ? "none"
        : "partial"
      : "uncertain";

  const labels = [
    ...([va.video_type, va.video_style].filter(Boolean) as string[]),
    ...(Array.isArray(fashionPlacement.recommended_styles)
      ? (fashionPlacement.recommended_styles as Record<string, unknown>[]).map((s) =>
          typeof s.style === "string" ? s.style.trim() : ""
        )
      : []),
  ].filter((l) => l.length > 0);

  const reason =
    typeof fashionPlacement.reason === "string" ? fashionPlacement.reason.trim()
      : typeof fashionPlacement.placement_notes === "string" ? fashionPlacement.placement_notes.trim()
      : "";

  return {
    ...(record as unknown as LlmReverseOutput),
    video_info: videoInfo as unknown as LlmReverseOutput["video_info"],
    video_analysis: videoAnalysis as unknown as LlmReverseOutput["video_analysis"],
    shot_breakdown: shotBreakdown as unknown as LlmReverseOutput["shot_breakdown"],
    editing_analysis: editingAnalysis as unknown as LlmReverseOutput["editing_analysis"],
    hot_trend_labels: {
      suitability,
      humanPresence,
      humanExposure,
      labels,
      reason,
    },
  };
}

export type LlmReverseOutput = VideoHotTrendAnalysisOutputFull & {
  hot_trend_labels: {
    suitability: HotTrendSuitability;
    humanPresence: HotTrendHumanPresence;
    humanExposure: HotTrendHumanExposure;
    labels: string[];
    reason: string;
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 normalize-output.ts 移动 normalizeLlmReverseOutput 函数</name>
  <files>src/modules/video-reverse-core/normalize-output.ts</files>
  <read_first>
    - src/modules/video-hot-trend/sync-service.ts (lines 103-166: normalizeLlmReverseOutput 函数实现，lines 72-80: LlmReverseOutput 类型定义)
    - src/modules/video-hot-trend/types.ts (VideoHotTrendAnalysisOutputFull 类型)
    - src/contracts/hot-trend-base.ts (HotTrendSuitability, HotTrendHumanPresence, HotTrendHumanExposure 类型)
  </read_first>
  <action>
创建 `src/modules/video-reverse-core/normalize-output.ts` 文件，从 sync-service.ts 移动 normalizeLlmReverseOutput 函数（per CORE-03, D-06）：

1. **导入类型**：
   - VideoHotTrendAnalysisOutputFull from "../../modules/video-hot-trend/types.js"
   - HotTrendSuitability, HotTrendHumanPresence, HotTrendHumanExposure from "../../contracts/hot-trend-base.js"

2. **定义 LlmReverseOutput 类型**（复制 sync-service.ts L72-80）：
```typescript
export type LlmReverseOutput = VideoHotTrendAnalysisOutputFull & {
  hot_trend_labels: {
    suitability: HotTrendSuitability;
    humanPresence: HotTrendHumanPresence;
    humanExposure: HotTrendHumanExposure;
    labels: string[];
    reason: string;
  };
};
```

3. **复制 normalizeLlmReverseOutput 函数**（sync-service.ts L103-166），保持逻辑不变：
   - 参数: raw: unknown
   - 返回: LlmReverseOutput
   - 逻辑: 提取字段、推导 hot_trend_labels、返回标准化输出
   - 注释: 保持原有中文注释

**不要修改 sync-service.ts 或 single-reverse-service.ts** — 后续 Phase 4 会更新导入路径。
此阶段仅创建核心模块文件，验证函数可独立导出。
  </action>
  <verify>
    <automated>grep -c "export function normalizeLlmReverseOutput\|export type LlmReverseOutput" src/modules/video-reverse-core/normalize-output.ts</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在且行数 >= 80
    - 导出 normalizeLlmReverseOutput 函数，签名 (raw: unknown) => LlmReverseOutput
    - 导出 LlmReverseOutput 类型
    - 导入 VideoHotTrendAnalysisOutputFull 类型
    - 导入 HotTrendSuitability, HotTrendHumanPresence, HotTrendHumanExposure 类型
    - 函数逻辑与 sync-service.ts 版本一致（hot_trend_labels 推导逻辑完整）
    - 包含原有中文注释
  </acceptance_criteria>
  <done>normalize-output.ts 创建完成，normalizeLlmReverseOutput 函数已移动到核心模块</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core module ↔ Existing types | Imports from video-hot-trend/types.ts, no modification |
| normalize function ↔ Raw input | Validates unknown input, safe type casting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Raw LLM input | mitigate | Function validates input structure, handles missing fields gracefully |
| T-01-04 | Information Disclosure | LLM output data | accept | Output contains video analysis data, no sensitive credentials |
</threat_model>

<verification>
- normalize-output.ts 文件独立存在
- normalizeLlmReverseOutput 函数可导出
- LlmReverseOutput 类型定义完整
- TypeScript 编译通过
</verification>

<success_criteria>
- normalize-output.ts 包含 normalizeLlmReverseOutput 函数
- 函数逻辑与 sync-service.ts 版本一致
- LlmReverseOutput 类型定义完整
- 文件可被其他模块导入（暂不更新导入，Phase 4 统一处理）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-core-pipeline-extraction/01-02-SUMMARY.md`
</output>